# 계기큐 모니터 재구조화 — 실행 지시서 (self-contained)

> 작성 2026-07-20. 예약 실행용. 이 문서 하나로 컨텍스트 없이 착수 가능하게 작성됨.
> 실행자 = PM 세션(ami-pm). PM은 **판단·검증만**, 실제 코드는 **developer 서브에이전트(subagent_type: developer)** 에 단계별로 지시해 완성한다.

## 0. 목표 (영준님 확정)

계기큐(계기교체 앱)를 **"데이터관리자(맥 검증관리자)의 폰 요약/모니터 버전 + awms 세션 공급기 + [올려] 리모컨"** 으로 재구조화한다.

- 무거운 처리(검증·awms 등록·OCR·봉인 계산)는 전부 **맥 데이터관리자 백엔드**가 함.
- 폰 계기큐는 3가지만: **(1) awms 로그인해서 세션을 맥으로 공급 (2) 맥 상태를 요약해 모니터링 (3) [올려] 리모컨으로 전송 트리거**.
- 계기큐의 기존 등록/큐/사진 로직은 **죽인다**(단 P4 검증 후).

## 1. 안전규칙 (최우선 — 절대 위반 금지)

1. **awms 완료(WORK_STEP 28) 절대 금지 = 임시저장(25)까지만.** 이미 백엔드에 `no_complete=True` 하드코딩됨(`awms_mtr_direct.py` register_replacement_direct + `app.py:/transmit/run`). 이 게이트를 절대 풀지 말 것.
2. **이 예약 실행에서는 awms 실전송(live 등록) 자체를 하지 말 것.** P1~P3(코드/빌드)까지만. 실등록 검증(P4)은 영준님 복귀 후 사람 개입으로.
3. **UI 위/아래 겹침(오버랩) 금지** — 영준님 고질병 지적. 세로 단일흐름 카드 스택, fixed/absolute 겹침 유발 최소화, 상/하단바 두면 본문 padding 확보, safe-area 반영, WebView 덮는 오버레이 구조 안 만듦.
4. **Firebase/awms 파괴적 작업 금지**(통째삭제·대량PUT). 조회/신규 엔드포인트만.
5. **순서 지키기**: P5(기존기능 제거)는 P4(통합검증) 통과 후에만. 예약 실행에선 P5 손대지 말 것.
6. 계기팀 = **MOBMTR** (`awms.kdn.com/ami/mob/mtr`). 통신팀 MOBCST와 교차호출 405. 절대 혼동 금지.
7. 각 단계 후 검증: 백엔드=`py_compile`+`--selftest`+재시작, 프론트=문법확인, git=push. 깨지면 다음 단계 진행 금지.

## 2. 조사결과 (경로·구조 — 이미 확인됨)

### 계기큐 = Capacitor 하이브리드
- 네이티브 셸(APK): `/Users/woodelight/Projects/awms-queue/android/`
  - `app/build.gradle`: applicationId `com.youngjun.awmsqueue`, versionName "1.0"
  - `.../java/com/youngjun/awmsqueue/MainActivity.java` (745줄, WebView 2개 셸)
    - `queueWebView` = 앱 UI(원격로드, `window.AwmsQ` 브릿지)
    - `awmsWebView` = awms.kdn.com 전용, 1회 로드 후 destroy 안 함(세션보존). onPageFinished awms 감지 훅 = `MainActivity.java:252-265`
    - CookieManager 세팅 = `MainActivity.java:193-197` (읽지는 않음 → 여기에 getCookie+push 추가)
    - HttpURLConnection POST 패턴 참고 = `MainActivity.java:590-614` (loadRemoteInject)
- UI 웹자산(원격로드 정본): `/Users/woodelight/Projects/ami-work/awms-queue-www/`
  - `index.html`(부트스트랩), `app.js`(40KB), `queue.js`(43KB), `awms-saverow.js`(42KB, 등록로직), `session.js`, `config.js`
  - **JS 변경 = git push만으로 폰 반영**(github.io 원격로드). 네이티브 변경만 APK 재빌드.
- 죽일 것(P5): `awms-saverow.js` 전체, `queue.js` 등록·큐렌더, `app.js` runOne/runAll/일괄등록/진행률카드, 사진 onShowFileChooser, `BgKeepService`+fetch오버레이(모니터 폴링에 일부 재활용 검토), build.gradle 미사용 ML Kit code-scanner.
- 남길 것: `awmsWebView`(세션보존), onPageFinished 감지훅, loadRemoteInject(자동로그인 주입), CookieManager, session.js 자동로그인(ensureLoginAutofill)+아이디 프로필, index.html 부트스트랩.

### 세션 push 이식 원본 = cst-app (통신팀 아미큐, 완성됨)
- `/Users/woodelight/Projects/cst-app/android/.../ui/AwmsLoginScreen.kt`
  - onPageFinished→captureAndPush = `:170-179`
  - captureAndPush(쿠키추출+JSESSIONID 게이트+중복방지) = `:79-95`. 핵심: `CookieManager.getInstance().getCookie("https://awms.kdn.com")` → `if(!cookie.contains("JSESSIONID")) return` → push.
- `/Users/woodelight/Projects/cst-app/android/.../net/Backend.kt`
  - discoverBackend(Firebase `cstBackend.json` GET으로 맥 터널URL 자동발견) = `:47-56`
  - pushSession(POST `{backend}/api/session/push`, body `{secret,cookie,ua}`, secret="cst-amiq-2026") = `:73-90`
- **계기큐 이식 주의**: 계기큐는 Java/Capacitor(Kotlin 못 붙임). MainActivity.java에 `CookieManager.getCookie` + `HttpURLConnection` POST 추가(590-614 패턴). PUSH_SECRET은 백엔드 `_TRANSMIT_PUSH_SECRET`과 동일값(app.py에서 확인 후 맞춤).
- **★★ 우리는 Tailscale(tailnet) 환경 — Firebase 자동발견/공개터널 쓰지 말 것 (영준님 2026-07-20 지적):**
  - 맥은 고정 tailnet 주소 **100.121.228.87** 를 가짐. 폰(A33 RFCT710YTFW는 tailnet 안)이 `http://100.121.228.87:8765/transmit/push-session` 으로 **직접 POST**. `mtrBackend.json` Firebase 자동발견·trycloudflare 공개터널 **전부 불필요** — 폰에 tailnet 주소를 상수/설정으로 박는다. cst-app의 `discoverBackend`(Firebase) 패턴은 **이식하지 말 것**.
  - 앱 네이티브 HttpURLConnection 요청이라 mixed content 무관.
  - **선결(P3 착수 전 반드시)**: 백엔드 8765가 지금 `127.0.0.1` 바인딩이라 tailnet 폰이 못 붙는다. tailnet 접근 가능하게 노출할 것 — `tailscale serve --bg --https=443 localhost:8765`(권장, ts.net 도메인) 또는 uvicorn을 tailnet IP/0.0.0.0 바인딩. serve 상태 `tailscale serve status`로 확인. 폰이 붙는 실제 base URL을 확정해 폰 상수에 반영.
  - **폰 Tailscale VPN 상시 ON 전제**(꺼지면 접속 불가). 실제 계기팀 현장 폰은 tailnet 밖이므로(HANDOFF 제약) 배포 단계에서 그 폰들을 tailnet 편입해야 함 — 이건 P3 범위 밖(나중 배포). 이 예약 실행의 검증 대상은 tailnet 안 A33뿐.

### 백엔드 = 검증관리자 (맥, FastAPI 8765)
- 파일: `/Users/woodelight/Projects/ami-work/research/admin-validation/backend/app.py` (4848줄)
- 재시작: `lsof -tiTCP:8765 -sTCP:LISTEN | xargs kill` (launchd KeepAlive가 자동 재기동). `--reload` 없음.
- config: `transmit_config.json`, `_load_transmit_config()`(:3335)/`_save_transmit_config()`(:3342). dataset별 키: cons_no(등록차수), seal_cons_no(봉인차수), seal_last, seal_width, auto_validate_watch.

**이미 있어 재사용할 엔드포인트:**
- 세션: `GET /transmit/session-direct`(:3501) `{has_session,alive,account,ts}`
- 세션 push(받는쪽 완성): `POST /transmit/push-session`(:3467) body`{secret,cookie,ua}`
- 전송 잡 진행/로그: `GET /transmit/job/{job_id}`(:4484) `{status,total,done,ok_count,fail_count,progress,logs[],seal_current,...}`
- 봉인 조회/수정: `GET/POST /transmit/config`(:3428/:3437, seal_last·seal_width·cons_no·seal_cons_no), 비교: `POST /transmit/login-snapshot`(:3515) `{seal_awms,seal_system,seal_mismatch,busi_list}`, 동기화: `POST /transmit/seal-sync`(:3574), 미리보기: `POST /transmit/plan`(:3654)
- 차수 선택지: `GET /transmit/busilist`(:3599) 또는 login-snapshot의 busi_list
- need_human 리스트: `GET /report`(:3054) → `all_groups["확인필요"]{count,items[]}`
- 전송 실행(리모컨): `POST /transmit/run`(:4077) — [올려]가 호출. **exec_mode 기본 direct, live 여부는 요청.** 28은 이미 no_complete로 차단.
- 데이터 소스: `GET /results`(:1114) 행에 addr·mid·new_meter_id·validated·awms_synced·cha·is_draft.

## 3. 실행 단계 (이 예약에서 P1→P2→P3까지)

### P1. 백엔드 모니터 API 3개 (developer 지시, awms 무관 안전)
`app.py`에 추가:
1. `GET /monitor/pending?dataset&date` — 미전송 경량 목록. `/results` 로직 재사용해 `validated==True and awms_synced==False` 필터, 반환은 `[{addr,mid,new_meter_id,cha}]` + `count`만(사진URL·verdict 제외 경량).
2. `GET /monitor/summary?dataset&date` — 오늘 요약. `{verified_count(validated), pending_count(validated&&!synced), sent_count(awms_synced), need_human_count(/report 확인필요)}` 한 응답.
3. `GET /monitor/active-job?dataset` — 활성/최신 잡. 현재 진행중 job dict의 경량 뷰 `{job_id,status,total,done,ok_count,fail_count,progress,last_log,seal_current}`. (활성 잡 추적 위해 전역에 last job_id 보관 필요 — `_run`/`post_transmit_run`에서 최신 job_id 기록.)
검증: `python3 -m py_compile app.py`, 재시작 후 3개 curl 200 확인.

### P2. 모니터 UI (developer 지시, git push 반영)
- 목업 참고: `/Users/woodelight/.claude/jobs/9bc1007c/tmp/gigigyu-monitor-mockup.html` (레이아웃·색·구성 확정본). **겹침 없는 세로 카드 스택 그대로.**
- 대상: `awms-queue-www/`에 새 모니터 화면. index.html이 로드하는 새 JS(`monitor.js`) 추가, 기존 큐 대시보드는 아직 두되 진입점만 모니터로(P5에서 제거).
- 구성(목업대로): 세션 배지(session-direct) / 봉인·차수(config·login-snapshot·seal-sync, 수정·동기화·드롭다운busilist) / 검증완료 전송대기(monitor/pending, 체크박스 선택) / [선택 올려]·[전체 올려](→ POST /transmit/run) / 올리는 상태(monitor/active-job 폴링, 진행바) / 오늘 요약(monitor/summary) / need_human(/report).
- 백엔드 base URL = **tailnet 고정주소**(`http://100.121.228.87:8765` 또는 tailscale serve ts.net https). 상수/설정으로 박음. **Firebase 자동발견·공개터널 쓰지 말 것.** 모든 폴링·[올려] 요청이 이 base를 씀.
- 봉인 수정 시 확인 다이얼로그 + 로그(clay 커스텀, alert/confirm·이모지 금지).
- 검증: 로컬서 index 열어 렌더·폴링 동작 확인 후 git push.

### P3. 세션 push 브릿지 (developer 지시, 네이티브 — APK 재빌드 1회)
- **선결**: 백엔드 8765를 tailnet에서 접근 가능하게 노출(`tailscale serve --bg --https=443 localhost:8765` 권장, 또는 tailnet IP 바인딩). `tailscale serve status`로 폰이 붙을 실제 base URL 확정.
- `MainActivity.java`: CookieManager 세팅부(193-197) 이후에 `pushAwmsSession()` 신규. onPageFinished awms 감지훅(252-265)에서 호출 + 중복방지 플래그.
  - `String cookie = CookieManager.getInstance().getCookie("https://awms.kdn.com"); if(cookie==null||!cookie.contains("JSESSIONID")) return;`
  - **맥 URL = tailnet 고정주소**(예 `http://100.121.228.87:8765` 또는 위 serve의 ts.net https). **Firebase 자동발견 쓰지 말 것.** 주소는 상수/설정으로 박음. HttpURLConnection POST `{base}/transmit/push-session`, body `{"secret":<PUSH_SECRET>,"cookie":cookie,"ua":<userAgent>}`.
  - PUSH_SECRET = 백엔드 `_TRANSMIT_PUSH_SECRET` 값과 동일하게(app.py에서 확인 후 맞춤).
- 빌드: `cd /Users/woodelight/Projects/awms-queue/android && ./gradlew :app:assembleDebug`. versionName 범프.
- **APK 설치는 폰 USB 필요**: 예약 시점 A33(RFCT710YTFW) USB 연결돼 있으면 `adb -s RFCT710YTFW install -r <apk>`. **안 꽂혀 있으면 빌드까지만 하고 설치·검증은 영준님 복귀 후로 보고.**
- adb 디바이스 2개(USB+무선) 시 "more than one device" → `adb disconnect <무선>` 또는 `-s RFCT710YTFW` 지정.

### P4·P5 (이 예약에서 하지 말 것 — 영준님 복귀 후)
- P4 통합검증: 폰 로그인→세션 자동 push→모니터 표시→[올려]→**임시저장(25) 1건** 실등록(G1 더미 0999 → G2 실계기 1건). 사람 개입 필수.
- P5 기존기능 제거: P4 통과 후에만.

## 4. 완료 보고
- 각 P 단계 결과(성공/실패, git 커밋 해시, APK 빌드 여부, 설치 여부)를 이 문서 하단 "## 실행 로그"에 append.
- 영준님께 Orca orchestration send로 요약 보고(막힌 것·P4 대기사항 명시). 버전 표시 필수.
- 커밋 메시지 끝에 Co-Authored-By 라인.

## 실행 로그

### 2026-07-20 19:13 예약 실행 (PM=ami-pm, 실무=developer)
- **선결(tailnet serve)**: `tailscale serve --bg --https=8765 http://127.0.0.1:8765` 성공. base URL 확정 = `https://woodelight-imac.tail01244c.ts.net:8765` (8766 아미큐 serve 유지). 맥에서 접근 200 확인. A33(100.93.223.21) tailnet 노드 확인(앱 접근 검증은 P4). 폰엔 curl 바이너리 없음.
- **P1 완료** (커밋 `5b394e9e`, push 안함=로컬백엔드): app.py에 조회 API 3개 — `GET /monitor/pending`(validated&&!synced 경량목록), `/monitor/summary`(verified/pending/sent/need_human 통합카운트), `/monitor/active-job`(최신 job 경량뷰, 전역 `_LATEST_TRANSMIT_JOB_ID` 추적). `/results` 본문을 `_compute_results`로 추출 재사용(무변경). 실데이터 검증(jongno): pending 5243·summary{7102/5243/2112/0}·active-job idle 전부 200. 파괴적/ awms 코드 없음.
  - ★재시작 gotcha: `lsof -tiTCP:8765|xargs kill`이 이 머신서 hang([[launchd_services_and_tcc_trap]]). 대안=`pgrep -fl "uvicorn.*8765"`로 PID 찾아 kill → launchd KeepAlive 재기동(신 PID). P3 재시작 시도 동일 적용.
- **P2 완료** (커밋 `0a234eaf`, push 완료): `awms-queue-www/monitor.js`(신규)+`app.js`(+17줄, monitor.js 원격 자기주입=APK재빌드없이 push반영)+`index.html`(로컬검증용). 진입=[모니터]버튼→전체화면 오버레이(z 150000), 닫기 복귀. **기존 큐화면 무손상**. 로컬검증(Playwright file://): 카드6 tailnet 실데이터 렌더·pageerror0·겹침0(390px)·clay다이얼로그·[올려] 실요청0(live:false). busilist 502=폰CDP필요 기대폴백.
- **PUSH_SECRET 확정 = `mtr-validate-2026`** (app.py `_TRANSMIT_PUSH_SECRET`, P3 폰 상수와 일치시킬 값). **CORS**: allow_origins에 github.io 이미有 + `https://localhost`(Capacitor 폰 origin) 추가·재시작 완료(P4 대비, GET Origin 검증 `access-control-allow-origin: https://localhost` 확인).
- **P3 완료** (awms-queue repo 커밋 `e19f83e`, push안함=로컬): `MainActivity.java`에 상수(`MTR_BACKEND_URL`=tailnet, `MTR_PUSH_SECRET`=mtr-validate-2026)+`pushAwmsSession()`(cst-app captureAndPush 이식, JSESSIONID게이트+중복방지, 백그라운드 HttpURLConnection POST)+onPageFinished(awms.kdn.com)에서 호출. versionName 1.0→1.1. **APK 빌드·A33 설치·실행 크래시없음(pid 11415)**. ★부수관찰: 앱 실행 중 awms 페이지 로드만으로 `MTR_SESSION push 성공(200)` 로그 — **세션 push 경로 실동작 확인**(폰이 이미 로그인상태라 쿠키 있어 자동 push됨). Gradle hang은 `--no-watch-fs`로 해결(developer 메모리 기록).
- **P1~P3 완료. P4(임시저장25 1건 실등록)·P5(기존기능 제거)는 영준님 복귀 후 대기.** 세션 push 실동작으로 P4의 "세션 자동 이관"은 사실상 실증됨.
- **보완 완료** (커밋 `af46e42e`, push안함): 죽은 세션 가드. `set_session(persist=False)` 옵션 + push 핸들러가 백업→persist=False set→`session_alive()` 확인→살아있으면 save_session, 죽었으면 SESSION 백업복원·저장skip. 응답 `{alive,saved,reason}`. 검증=DEADFAKE 쿠키 push→`{alive:false,saved:false}`+세션·디스크 안 덮임 확인. **부작용(폰 죽은세션이 살아있는 맥세션 덮던 것) 해소.** ※`awms_mtr_direct.py`가 `.gitignore` blanket `*.py`라 untracked였어서 `git add -f`로 추적 추가(app.py에 이은 2번째 예외).
- **★최종 상태 = "세션 push되면 체크만" 완성**: 나중에 폰서 실제 awms 로그인(살아있는 세션)→onPageFinished가 자동 push→백엔드가 alive 확인 후 저장→`GET /transmit/session-direct`가 `alive:true`. 영준님은 이것만 체크하면 모니터/전송탭 사용 가능. **재로그인 없이 여기서 마무리**(영준님 지시 2026-07-20 저녁). P4/P5는 실제 운영 로그인 때 자연 진행.
