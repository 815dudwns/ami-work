# awms FMPMTR 엔드포인트·필드 사전 (실측 2026-09-18, 통신팀)

조회 전용으로만 확인했다. **쓰기 API(saveRow/saveAct 등)는 호출하지 않았다.**

## 1. 엔드포인트 탐색 결과

| 경로 | 결과 |
|---|---|
| `/ami/fmp/mtr/fmpMtr1000/selectList` | **실동작** — 연간대상 실효계기 목록 |
| `/ami/fmp/mtr/fmpMtr2000/selectList` | 200 OK 이나 **항상 빈 배열**. 파라미터 조합 미상 |
| `fmpMtr2000/{getList,selectDetail,getDetail,selectMain,getMainList,selectYearList}` | 404 |
| `fmpMtr3000~8000/selectList` | 404 |
| `fmpMtr1000/getCommonCode` · `fmpMtr2000/getCommonCode` | 404 (`mob` 계열에만 있다) |

- 앱/메뉴: `app=FMPMTR` · `menu=01010000` (Referer 에 그대로 넣어야 한다)
- 인증: `Cookie: JSESSIONID=...` 단독. 계정은 윤용운 반장 계열(FMPMTR 은 그 계정만 열린다)
- `index.html` 은 껍데기다 — 화면은 JS 로 그려지고 메뉴 코드가 HTML 에 없다.
  다른 메뉴 코드는 **이 방법으로는 못 찾았다**(미해결).

## 2. `fmpMtr1000/selectList` 파라미터

```
dept1=3970            서울지역본부(고정)
dept2=<지사코드>       빈 값이면 전 지사가 아니라 0건이 나온다 — 반드시 지정한다
workStrDate=YYYYMMDDHHMM / workEndDate=YYYYMMDDHHMM
searchVal=<계기번호>   ★철거계기(WHM_NO)로만 검색된다. 신설계기(WHM_NO10)는 0건
pPageNo=1 / pRowCount=<최대>   100000 까지 받아진다
wrkCl / lvHvCl / wrkYn / wrkStep / loginBupeId / sortKey / matchYn=N   (빈 값 허용)
```

지사코드: 노원도봉 3400 · 광진성동 3100 · 강북성북 4080 · 서울본부직할 7793 ·
마포용산 3600 · 동대문중랑 3000 · 서대문은평 3500

**★성능**: 건별 `searchVal` 조회는 **건당 약 8초**다(1,631건 = 2.6시간).
지사 통째 조회는 **전 기간 14,534행을 13.5초**, 7개 지사 전체 91,216행을 **25초**에 준다.
대량 조회는 반드시 **지사 통째로 받아 로컬 색인**으로 조인하라.

## 3. 응답 필드 사전 (28개, 실측)

| 필드 | 뜻 | 예 |
|---|---|---|
| `CONS_TGT_SEQNO` | 시공대상 순번 — **행 유니크 키** | 8991279 |
| `CONS_NO` | 공사번호 | 310520263017 |
| `WHM_NO` | **철거계기**(교체 전) — `searchVal` 이 찾는 열 | 73450002617 |
| `WHM_NO10` | **신설계기**(교체 후, 현재 달려 있는 것) | 08550137763 |
| `WRK_PLCE_ADDR_CTT` | **작업장소 주소** — `도로명(지번,동호수)` 혼합 표기 | 서울특별시 성동구 용답동 235-1 201호 |
| `CNTR_NO` | 고객번호 | 0112057808 |
| `WORK_DATE` | 작업일시 | 2026-09-17 10:24 |
| `WORK_STEP` | 진행단계(28=완료) | 28 |
| `WORK_YN` | 작업여부 | Y |
| `BUPE_ID` / `BUPE_NM` | 업체 | 1148101670 / 금광전설(주) |
| `CNTR_CLAS_CD` | 계약종별 코드 | 211 |
| `CNTR_PWR` | 계약전력 | 10 |
| `DIST_LV_HV_CLCD` | 저압/고압 구분 | 1 |
| `OFFICE_CD` / `HDQR_CD` | 사업소 / 본부 | 3100 / 3970 |
| `LAY_STS_CD` · `LAY_METR_CL_CD` · `BLON_CL_CD` · `PACH_CLCD` · `GAETONG_YN` | 부설·소속 구분 코드 | |
| `WRK_STRT_SCHL_DT` · `WRK_END_SCHL_DT` | 예정일(대개 빈 값) | |
| `MOD_ID` / `USER_NM` | 최종 수정자 | mdp2601599 |
| `WHM_SEQNO` · `CNT` · `RNUM` | 내부 순번 | |

**★모뎀 MAC 은 없다.** 이 화면으로는 MAC 조회가 불가능하다.

## 4. 이 화면으로 주소를 회수하는 법 (2026-09-18 실사용)

우리 계기번호(= 우리가 모뎀을 단 시점의 계기)로 찾으면,
- **철거계기(WHM_NO) 매칭** → 우리 계기가 그 자리에서 뽑혀 나간 기록 = **우리 개소 주소**.
  계기가 재사용돼 딴 데로 간 것은 그 **뒤** 이야기라, 재사용 계기일수록 이 매칭이 정확하다.
- **신설계기(WHM_NO10) 매칭** → 두 가지가 섞인다.
  그 계기가 25년에 우리 개소에 설치된 기록이면 맞고(**우리 시공일 이전**),
  26년 재사용으로 남의 개소에 설치된 기록이면 틀리다(**우리 시공일 이후**). 작업일로 가른다.
- `CNTR_NO` 가 우리 고객번호와 같으면 확정(A등급).

정답지 검증(원장 주소 보유 계기 기준, 고객번호 미사용 조건):
`신설계기·시공일이전` 오염 **1.9%**(판정 259 중 5, 그중 3건은 도로명/지번 표기차 오판이라 실질 0.8%) ·
`철거계기` **0%** · `신설계기·시공일이후` 1/1 오답.
참고로 보강현황을 계기번호로 붙였을 때의 오염은 **11~18%** 였다.

구현: `scripts/fetch_awms_addr_20260918.py` (`--reuse-raw` 로 세션 없이 재판정)
