# 발주 — 25미청구 · 25년 미청구불가 리스트업 (PM → 통신팀, 2026-09-20)

## 작업의 실체
지금 달려 있는 모뎀을 **26년 공사 모뎀으로 갈아끼운다**(맥갈이). 그래서 화면은 두 층으로 나뉜다:
**상단 = 새로 달 모뎀이 붙을 곳(DCU 대장 기준) / 디테일 = 지금 달려 있는 것(원장·회신 기준).**

**★판단 유도는 하지 않는다**(영준님 2026-09-20). "PLC 로 재시공 검토" 같은 배지·경고를 붙이지 마라.
변대주 필드에 **정확한 정보만** 넣으면 작업자가 알아서 고려한다. 리스트가 틀렸다고 판단하면
작업자가 알아서 시공한다 — 그건 작업자 판단이다.

---

## 0. 먼저 — 변대주 3축 교정 (게이트 실패 상태다)
`python3 scripts/validate_bdju.py data/michunggu-data.json` 이 지금 **NG 1,555건**을 낸다:
이름 칸에 번호 903 · DCUID 574 · 기타 78. 출처는 전부 한전 회신 붙이기(고객번호 키 770 · MAC 키 707).
전산화번호를 이름 칸에도 복사한 것이다.

- **형태로 판정해 제자리에 넣어라**: 한글=이름칸 / 8자리=번호칸 / 10자리=DCUID칸 / 그 외는 버린다.
- 이름 칸을 비운 뒤 **`dcu_master` 에서 진짜 이름을 가져와 채운다**(3축 각각으로 조회).
- **이름 비교는 공백만 제거**(대장 고유 18,993, 공백 제거 충돌 0). **저장은 원문 그대로.**
- `DCUID 앞 8자리 = 전산화번호` 는 대장 19,007건 예외 0 인 **계산**이라 유도해도 된다(조회와 다르다).
  이 규칙으로 `DCUID 있고 번호 없는 1,069건`을 채워라. 출처에 `규칙/DCUID앞8자리` 를 남긴다.
  ★둘 다 있는데 어긋나는 135건은 **원장 번호를 남기고** DCUID 파생은 `_대안` 으로만.
- **빌더 마지막에 `validate_bdju.py` 를 돌려 실패하면 산출물을 쓰지 마라.**

## 1. 리스트 2종

### (A) `25미청구`
- 대상: 현재 `data/michunggu-data.json` 8,326건(주소 보유분). 주소 없는 193건은 올리지 않는다.
- `js/datasets.js` `uiLabel` 을 **`25미청구`** 로 바꾼다(지금 `25년 미청구`). category·code 는 그대로.

### (B) `25년 미청구불가` — 신규
- 대상: 25년 불가(고압 제외) 중 **분류 '대상' 5,856계기**에서 중복 157 제외 → **5,699**.
  판정 정본 `/Users/woodelight/.claude/jobs/93068d24/tmp/class_final.json`(사유 조합별 대상/제외).
  ★이 파일을 `research/불가사유_분류_확정_20260920.json` 으로 repo 에 옮겨 커밋해라(스크래치는 휘발된다).
  중복 제외축 = modem_work(20260908·20260920) · site-data · 합동 아카이브 · 미청구 대상.
- **주소 있는 것만 올린다**(주소 99.9%, 결손 3건은 제외 목록으로).
- 좌표·DCU 태그·데이터셋 등록은 미청구와 같은 절차. code 는 미사용 문자로(예 `u`),
  `label: null`(마커에 계기 개수), `allowGroup: 'staff'`, `statsEvenIfGroup: true`.

## 2. 필드 배치

### 상단(변대주 영역) — **`dcu_master` 값 그대로**
```
변대주명 · 변대주 전산화번호 · DCU ID · DCU 통신방식(인입망) · 회선상태 · 철거판정
```
- 3축 각각으로 조회(DCUID → 전산화번호 → 이름 순). 매칭 5,891/8,519(69.2%).
- **대장에 없으면 빈칸**으로 두고 출처에 `대장미등재` 를 남긴다. 원장 값으로 대체하지 마라.
- `철거판정` 은 `해지|유지|미판정` 을 **그대로** 적는다. 해석·경고 문구를 붙이지 마라.

### 디테일 — 현재 설치 현황
```
현재 모뎀 MAC · 현재 통신방식 · 계기번호 · 계기타입 · 고객번호 · 주소 · 도로명 · 공동주택명 · 상호
M/S · 집단 · 485타입 · 케이블 · 인입주 · 계약종별 · 검침방법 · 최종시공일 · 구분
★W(25대상)  ★최신 LP(09-13)
```
- **통신방식이 두 군데 나오므로 라벨을 가른다**: 상단 `DCU 통신방식` / 디테일 `현재 통신방식`.
  뜻이 다르다 — 상단은 DCU 인입망(PLC·K-DCU·HPGP), 디테일은 계기–모뎀 구간(KS-PLC·LTE·Zigbee·SMGW-C).
- **빼는 것**: 시공자(영준님) · 커넥터(2.1%·전부 '1') · 신호레벨(0%) · 앱변대주(0.1%).
- W·LP 는 `25년미청구분_v2`(snapshot 20260920)에서 고객번호 우선·계기번호 보조로 붙인다.
  매칭 8,229계기. W 값 = 신규시공 6,383 · 앱작성 신설 2,262 · 확인필요 92.

### (B) 불가 리스트 디테일 — 추가 필수
```
★불가사유(비고1) · ★불가상세(비고2)
```
- 모뎀 MAC 은 **넣지 마라** — 불가 건은 MAC 칸에 계기번호가 들어가 있다(11,009/11,015).

## 3. 주 과장 요청 엑셀
두 리스트에서 **주소 미확인 + 변대주 미확인**을 합쳐 한 장으로:
`research/주소변대주_요청_주덕기과장_20260920.xlsx`
- 열: 계기번호 · 고객번호 · 지사 · 주소(아는 것만) · 요청항목(주소/변대주/주소+변대주)
- 미청구 주소결손 193 + 변대주결손분 + 불가 주소결손 3 + 불가 변대주결손분
- 셀서식 텍스트 고정(앞 0 보존). 건수는 산출 후 보고.

## 3.5 합동시공 리스트 업데이트 (같이 돌려서 한 번에 배포)
마지막 수집이 **9/17** 이다(`data/inbox_hapdong/last_fetch.json`). 3일치가 밀렸다.
- 세션: 지금 살아 있다(`curl http://127.0.0.1:8766/api/session` -> alive:true). **절대만료 3.5h 이니 먼저 해라.**
  이식은 `~/.claude/skills/hapdong-update/SKILL.md` §1(cst-input/backend/session.json -> ~/.awms-tokens/fmpmtr.json).
- `python3 scripts/hapdong/fetch_awms.py` (인자 없이 = 마지막 수집일~오늘). ★날짜 하나만 조회하지 마라 —
  밤 22~24시에 올라오는 물량이 통째로 빠진다([[hapdong_collect_window_rule]]).
- 빌더로 `data/hapdong-data.json` 재생성, **영업일 3일 지난 분은 `hapdong-data-archive.json` 에 append**.
- ★합동은 변대주를 가져올 방법이 없다 — **`validate_bdju.py` 게이트 대상이 아니다**(영준님 2026-09-20).
  변대주 칸을 억지로 채우지 마라.
- 수집 로그에서 `[저장] N건 (이전 M -> +K)` 를 확인하고, **줄었으면 멈추고 보고**해라.

## 4. 게이트 (납품 전 전부)
- `python3 scripts/validate_bdju.py <두 산출물>` **통과 필수**
- 지도+대기 합 = 대상 수 · 계기번호 차집합 양방향 0 · 접두 보존 건수 · 좌표 null 0 · 중복 0
- `gen_stats_index.py` 는 **PM 이 main 에서** 돌린다. 데스크는 손대지 마라.
- **push 금지.** 커밋까지.

## 보고
`run:run_48e996df65bd` 로 건수 + 경로 + 게이트 결과.
