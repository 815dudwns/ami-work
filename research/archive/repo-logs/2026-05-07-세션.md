---
date: 2026-05-07
tags: [ami-work, ami-board, session-log]
project: AMI
---

# 2026-05-07 세션 로그 — ami-work / ami-board

## ami-work

### 계정 추가
- 장성훈(user05), 조은규(user06), 장진교(user07), 이규재(user08), 윤용운(user09), 이종우(user10)
- 비밀번호 모두 1111

### 버그 수정: 체크박스 영구 유지
- ami_checked_meters 별도 localStorage 키에 체크 상태 저장
- Firebase 30초 sync가 덮어써도 복원됨

### 기능: 계기번호 검색창
- 불가 사유 아래 검색창 추가 (inputmode=numeric, 숫자 키패드)
- 2자리 이상 입력 시 앞뒤 어디든 매칭 필터링
- 입력 시 meters-list로 자동 스크롤 (키보드에 가림 방지)
- X 리셋 버튼 추가

### 버그 수정: 완료 상태 풀리는 문제 (구조적 해결)
- 원인: saveStatus()가 전체 workStatus를 set()으로 교체 → 비동기 레이스 컨디션
- 해결: 이벤트 큐(ami_event_queue) 기반 구조로 전면 리팩토링
  - 체크/언체크/상태변경 각각 독립 이벤트로 기록
  - Firebase는 필드별 update() 사용 (set() 완전 폐기)
  - 30초 주기 / 창 활성화(visibilitychange) / 앱 시작 시 큐 flush
  - 창 닫아도 localStorage 큐 보존 → 다음 접속 시 재전송
  - 같은 대상 중복 이벤트는 ts 최신 우선

### 변대주 데이터
- DCU ID → 변대주 매핑: 대상리스트_260428.xlsx에서 추출
- 알파벳 포함(0000X0000 형태): 뒤 2자리 seg-dup 색상 강조, 복사 시 뒤 2자리 탈락
- 숫자만: 색상 없이 전체 표시, 전체 복사
- site-data.json + ami_data_coords.json + Firebase 업로드 완료

### UI 개선
- 액션 버튼 행 / 검색 행 분리 (flex-direction: column)
- 창 활성화 시 즉시 Firebase 동기화

## ami-board

### 출력물 주소 잘림 수정 (compose.js)
- 테이블 폭 95% 상한 제거 → 가장 긴 값(주소) 길이에 맞게 자동 확장
- truncateText → fillText

### autofill GPS 덮어쓰기 수정
- 지도에서 핀으로 픽한 주소(텍스트박스에 값 있음)가 있으면 GPS로 덮어쓰지 않음
- 빈칸일 때만 GPS 주소 사용

### CSS 레이아웃 시도 후 원복
- slot-cards 그리드 변경 시도했다가 레이아웃 깨져서 44bbdf6 시점으로 원복
