# HANDOFF — ami-work(아미맵) / jongno-combined(종로맵) / 계기큐·아미큐·헬퍼

> ★ **이 문서는 영구 상태판이다. 휘발성 세션 진행메모를 여기 쓰지 말 것** — 작업 디테일·과정은 메모리(`~/.claude/.../memory/`)와 `research/` 문서에 남긴다.
> ami-work 세션이 여러 개라 충돌하므로, HANDOFF는 **잘 안 바뀌는 것**(미완 트랙·대기 액션·핵심 규칙)만 단일 권위로 유지한다. **완료·배포·검증된 건 여기 남기지 말고 즉시 지운다**(상세는 옵시디언 로그·메모리).
>
> 앱 통칭 [[app_naming_convention]]. 배포 시 APP_VERSION 갱신 [[jongno_app_version_deploy]]. 종로맵=Naver Maps.
> **awms API 모를 때 = `research/awms-poc/awms_API_레퍼런스.md`. 통신팀(mob/cst)/계기팀(mob/mtr) 별개 — 항상 구별.**

## 현재 상태 (2026-09-17)
- **마지막 작업**: **LP 무기록 리스트 신설**(admin+윤용운 전용, 2,054건) + **완료 아카이브 8월 구간 복구**
  (17,473건, 통계 분모 26,204→43,677) + **통계 작업자별 개소 버그 수정** + 합동 리스트업 3회(9/15·9/16·9/17 서은).
- **진행 중**: **오늘 저녁 합동 전지사 재수집 + 9/14 백업**(아래 `다음 0`). 낮에 서대문은평 15건만 선반영해 뒀다.
- **배포 상태**: ami-work `f5ba7b6` / 아미맵 map.html **v20260917.1** / 아미큐 **2.2.18** /
  헬퍼 inject **v85**·앱 1.0.77 / 종로맵 20260813.3.
- **지도 데이터**: 실효 3,096(지번 1,346곳) · **합동 1,111**(9/14 304·9/15 395·9/16 397·9/17 15) ·
  **LP무기록 2,054**(admin·user09 만) · SKT 41. 통계 분모 55,732(아카이브 43,677).
- **데스크**: 통신팀(`tongsin`) 가동 중, 이번 구간 배포 전량 수행. 상세 `HANDOFF-통신팀.md`.

### ★이번 구간에 새로 만든 규칙 (되돌리지 말 것)
- **★보강현황 계기·통신방식·MAC 열은 철거/신설 쌍이다** — 접미 없는 쪽이 **철거**, `_2` 가 **신설**(지금 달려
  LP 를 올리는 것). 지도에 실을 것은 신설이다. 2026-09-15 에 PM 이 접미 없는 열로 발주해 1차 배포분이
  철거계기를 싣고 나갔다. **통신방식을 보고할 때 어느 쪽 기준인지 반드시 밝혀라** — 같은 2,126건이
  철거 기준 PLC 983·SMGW-C 63 / 신설 기준 SMGW-C 1,857·PLC 105 로 정반대다([[boranggi_meter_column_pair_removed_new]]).
- **★보강현황 15·16자리 MAC 은 두 가지 인코딩이다** — ①바이트=십진 한 자리 ②바이트=ASCII 숫자(16진).
  복원하면 awms `012`+8자리 표기가 된다. **하나만 구현하면 2,126건 중 905건이 조용히 '복원 불가'로 빠진다**
  (PM 이 실제로 그렇게 틀렸다). 정본 구현은 `scripts/build_lpnoapp_data.py`([[boranggi_mac_restore_and_amigo_wireless]]).
- **★아미고(보안계기)는 무선이라 옆집 모뎀이 LP 를 끌어간다**(영준님 2026-09-15). "우리 MAC 인데 등록
  계기번호가 다름" 656건 중 **아미고 584건은 오류가 아니라 정상**이다. 조치 대상은 AE·G 72건뿐.
- **★"우리가 했나" 판정은 Firebase `workStatus` 로 한다. 완료 아카이브로 판정하지 마라** —
  8월 구간 아카이브가 아예 없어서 누락이 크다(42건 표본에서 아카이브 9 vs Firebase 31).
  반대로 작업 기록 없이 아카이브에 들어간 건도 38건 있다([[completed_archive_incomplete_use_firebase]]).
- **★주소 단위 workStatus 로 계기 완료를 판정하지 마라**(영준님 2026-09-15). 한 주소에 계기가 여럿이면
  상태를 공유한다. 실측: 홍제동 131-2 는 6/25 에 G타입 1건을 완료한 기록이 **7/13 에 교체된 아미고 29건까지
  덮고** 있었다. 계기 단위 근거(MAC+awms 시공기록, 아카이브 계기번호 일치)만 써라.
- **★통계 인덱스는 main 워크트리에서만 생성한다** — `archivesGlob` 이 워크트리 기준이라 데스크에서 돌리면
  **없는 파일이 조용히 0으로** 잡힌다. 지금은 `gen_stats_index.py` 에 축소 시 `exit 1` 가드가 있다.
  **데스크는 데이터·배포만 하고 인덱스는 PM 이 돌린다**(보고에 지도건수+아카이브건수+합을 받아 대조).
- **★합동 백업은 `data/hapdong-data-archive.json` 에 누적(append)한다.** `datasets.js` 의 h 항목이
  그 **고정 경로 하나만** 읽는다 — 새 파일명으로 빼면 통계가 영영 못 읽고, 덮어쓰면 기존분이 날아간다.
- **★합동 보존은 영업일 3일이다**(달력 3일 아님). 9/17(목) 기준이면 9/17·9/16·9/15 가 남는다.
- **★계기번호 영문자 접두를 지우지 마라** — `A0530188699`·`LA530151258`. 정규화는 영문자가 섞였으면
  그대로 두고, 순수 숫자일 때만 `zfill(11)`([[meter_no_prefix_preserve]]).
- **★리스트업 산출물은 반드시 원본과 대조하고 배포한다.** 계기번호 집합 차집합 양방향·접두 문자 건수·좌표 null.
- **★Firebase `workStatus` 직접 수정 시 `syncAt` 필수**([[firebase_workstatus_syncat_required]]).
- **★한전 엑셀 `visible` 적재 완료** — `xlsx_to_db.py` 가 sheet XML 의 `hidden="1"` 을 읽는다.
  `WHERE visible='1'` 한 줄로 "그 판의 우리 대상"이 나온다. 숨김행(`visible='0'`)은 한전이 뺀 것이다.
- **★SMGW-C 는 무선 수집** — 근처 게이트웨이가 주변 계기를 끌어간다([[smgwc_wireless_pickup]]).
- **★합동 수집 구간 = 지난 수집 시점 ~ 이번 수집 명령.** 마포용산·직할은 22~24시에도 올린다.
  실측 9/15: 저녁 수집 389 → 다음날 재조회 405(+16, 전부 마포용산)([[hapdong_collect_window_rule]]).
- **★고압은 계약종별로만 걸러진다 — 목록은 awms 코드표에서 읽는다**(`p_code='CI007'` 중 명칭에 '고압').
  `DIST_LV_HV_CLCD` 는 전 건 저압으로 온다. **빈 필터로는 진행하지 않는다.**
- **★DCU 매칭은 변대주명 정확일치 + 유일할 때만.** 유사매칭 금지(2026-08-12 864건 오염).
  회선상태 해지·정지면 통신방식을 비우고, 대장에 근거 없는 값도 비운다. 정본 `scripts/fix_amimap_dcu_by_rule.py`
  (이번에 git 추적에 넣었다 — 그전엔 main 워크트리에만 있는 미추적 파일이었다).
- **★철거예정 DCU 는 회선상태로 안 걸린다** — 철거예정 175곳이 전부 '개통'이다. `apply_dcu_status.py` 로
  `dcu_철거예정` 을 따로 붙여야 하고([[dcu_removal_tag_rule]]), 통신방식이 PLC 로 붙는 것과 충돌하면
  상세모달에 "현재 PLC 로 적혀 있으나 LTE 로 시공"을 표시한다.
- **★아미맵 실무는 통신팀 데스크에 발주한다** — PM 이 직접 하지 마라. 발주서는 `/tmp/relay/<주제>-<날짜>.md`
  에 쓰고 경로만 보낸다. **그날 main 이 바뀐 내용을 발주서에 적어라**([[pm_delegate_amimap_to_desk]]).
- **데이터셋은 `js/datasets.js` 한 곳에서 정의한다**(파이썬 거울 `scripts/datasets.py`, parity 테스트로 보호).
  **지도에서만 내릴 때는 주석 처리가 아니라 `onMap:false`**.
- **계정 접근 권한은 `js/auth.js` `AUTH_GROUPS` 한 곳에서 정의한다**(이번에 신설). `staff=['admin','user09']`.
  레지스트리에는 **그룹 이름만** 두고 계정 목록을 적지 마라 — 출처가 둘이 되면 어긋난다.
- **여러 파일을 순서대로 로드하는 JS 는 합쳐서 실행해 봐야 한다** — `node --check` 는 `const` 중복선언을 못 잡는다.

### 다음 (우선순위)
0. **★오늘 저녁 — 합동 전지사 재수집 + 9/14 백업** (영준님 "있다 저녁에 다시 할 거야. 일을 땡겨서 하려고")
   · 낮 12:29 수집분은 **86건뿐**이고 노원도봉 0·마포용산 26·직할 28 로 미완성이었다. 서은 15건만 선반영.
   · 저녁에 `fetch_awms.py` 정규 실행(구간 수집이라 9/17 재조회로 낮 이후분이 자동 포함) →
     **전 지사 반영 + 9/14(304건) 백업**을 한 번에. 통계 인덱스는 PM 이 main 에서.
   · 빌더의 `--no-archive` 와 지사 선별은 **일회성 옵션**이다. 정규 실행하면 무필터·보존적용으로 돈다.
1. **★LP 무기록 조사 결과 — 결론만 남았다.** 2,126건이 이렇게 갈렸다:
   · **아미고 무선 픽업 1,924** (조치 불요)
   · **awms 시공 확인 72** (지도에서 제외 완료)
   · **Firebase 에 완료·불가 기록 42**
   · **★진짜 미상 88건** — MAC·계기번호·Firebase 어디에도 우리 흔적이 없는데 새 모뎀이 붙어 있다.
     마포용산 36·동대문중랑 22·노원도봉 14 / LTE_IV 44·PLC 35·K-DCU 7. 목록 `/tmp/relay/LP무기록_진짜미상_20260915.json`
   · 그와 별개로 **awms 계기번호가 어긋난 72건** 중 **확정 오타 17**(앞 2자리만 다름, 오독 쌍 7↔0·9↔6·2↔3)
     + **슬레이브 누락 39**(같은 모뎀의 연번 계기). 목록 `/tmp/relay/awms_수정대상_아미고제외_20260915.json`
   ★남은 선택: ①88건을 주덕기 과장에게 물을지 ②오타 17건 awms 수정 / 슬레이브 39건 추가 등록을 할지(권한 확인 필요)
2. **아미큐 교체(M1020) 실전 검증** — 2.2.18 배포했으나 awms 왕복 미확인.
3. **맥 오류 검사 실전 반응 확인** — 아미큐 2.2.18·헬퍼 v85 배포했으나 현장 오탐 여부 미확인.
4. **보류 사유를 필수로 할지 (미결)** — 지금은 선택. 불가는 종전대로 필수. 영준님 결정 대기.
5. **PLC 개소 DCUID 입력 표시** — 주덕기 지시 이행, 미구현
6. **아미큐 미결 2건** — ①앱 시작 시 업데이트 자동확인 ②EXIF 촬영시각 자동정렬
7. **KT모뎀 현장회수 137건** — 우리 개소와 겹치는지 미확인
8. **세션 없이 완료 눌리는 구멍** — `updateStatus()` 세션 가드 없음

### 참고 — 이번 구간 실측 (근거 필요할 때)
- **LP 수신 시각은 알 수 없다** — 보강현황 `최초LP 수신일`·`연계 수신일` 이 **77,128건 전부 `00:00:00`**.
  한전은 날짜만 준다. "며칠 만에 붙었나"만 비교 가능하다.
- **시공→LP 간격은 통신방식이 가른다** — 같은 날 작업분인데 SMGW-C 는 당일, KS-PLC 는 +38·+86일.
  박성준 같은 날 46.8% / 안지홍 35.2%, 안지홍의 +8일 이상 14.1%(박성준 5.1%)는 구역 통신방식 구성 차이로 보인다.
- **작업자 정보 없는 workStatus 67건(0.3%)** — 옛 형식이라 `updatedBy`·`updatedAt` 자체가 없다.
  완료 39·미착수 22. **누가 했는지 복구 불가**(시각도 없다). 371건은 시각만 있고 작업자 없음.
- **보강현황 판별 대상 추이**: 5/13 16,490 · 5/28 17,864 · 6/29 22,517 · 7/3 16,485 · 7/30 10,679 ·
  8/28 6,488 · **9/8 3,096**. 보이는 행은 어느 판이든 100% LP 미수신.
- **지사별 앱 작성 시간대** — 서대문은평 8~16시 고르게 / 노원도봉 13~15시 / 동대문중랑 10~11·14~16시 /
  **서울본부직할 16~17시+22시대** / **마포용산 17~23시+자정**.
- **작업자 속도(조 단위, 개소/일)** — 김영진+이규재 44.9 · 우희근+정수화 36.4 · 김지호+장성훈 35.5 ·
  김상권+윤용운 32.8 · 우영준+장진교 31.5.
- **1차 DCU(접미 00·01)는 성공률이 무너진다** — 2453 KS-PLC 8.3%, 4553 0%. "1차는 원래 안 된다"(영준님).
- **DCU_ID 누락은 LP 실패 원인이 아니다** — 변대주 있고 DCUID 없는 건 89% vs 있는 건 90%.

### ★데이터관리자 설계 확정사항 (유지)
- **봉인**: 7자리 고정 · 박스 범위는 설정 입력 · **계정 무관 팀 자원**(전역 커서) · 다음번호 = 우리 DB 마지막+1,
  awms 조회값은 범위 안이면 반영·밖이면 무시 · 삼상 N,N+1 · 박스 끝 1개 남으면 버림
- **차수**: 하드코딩 금지, 목록 선택. 기본값만 활성차수 자동추종. 화면엔 공사명+차수
- **전송**: 완료(28) 기본 · 검증 자동 대조 · 완료건 읽기 전용 잠금
- **환경**: 봉인 커서 **0315875** / 활성 26차(397820263354) / 계정 mdp2603726 / 백엔드 8765 launchd 자동재기동

### 데스크 상태
| 데스크 | 브랜치 | 상태판 |
|---|---|---|
| 통신팀 | `tongsin` | `HANDOFF-통신팀.md` — 이번 구간 배포 전량 수행(LP무기록 신설·DCU교정·아카이브 복구·통계 개소 버그·합동 3회). 저녁 합동 전지사 반영 대기 |
| 계기팀 | `gyegi` | `HANDOFF-계기팀.md` |
| 검증팀 | `geomjeung` | `HANDOFF-검증팀.md` |
| ocr-meter | `ocr-meter` | `HANDOFF-ocr-meter.md` |

- **★데스크 상태판 파일명을 갈랐다.** 데스크는 `HANDOFF-<팀>.md`, PM 은 `HANDOFF.md`. **되돌리지 마라.**
- **PM 우편함 `run:run_48e996df65bd`**(`~/.claude/orca-runs.json`). 고아 Run `run_f566b669c5ef` 미회수.
- 워크트리·워크스페이스 삭제 금지(경로 공유 형제 세션 전멸, 2026-07-18·07-19 실증).
- **PM 워크트리에서 `git pull` 이 자주 막힌다** — `data/inbox_hapdong/last_fetch.json` 이 수집 때마다 바뀌고
  데스크도 같은 파일을 커밋한다. 내용이 같으면 `git checkout --` 로 버리고 pull 하면 된다.

## 대기 액션 (영준님 결정/확인 필요)
- **★LP 무기록 진짜 미상 88건 — 주덕기 과장에게 물을지 미정.** 42건은 한전이 우리 대상으로 줬던 것인데
  우리 기록 없이 모뎀이 달렸다. 나머지 46건은 애초에 우리 대상으로 온 적도 없다.
- **★awms 계기번호 오타 17건 수정 / 슬레이브 누락 39건 등록 — 할지 미정.** 우리 작업건이라 실적이 회복되지만
  완료(28) 건 수정 권한 확인이 필요하다([[awms_others_records_no_edit]]·[[awms_saveact_completed_no_edit]]).
- **★주덕기 과장에게 물을 것(기존)** ①Y열에 모뎀 맥이 찍힌 36건 ②중랑구 면목동 23-3 계기 1개(24530088997)
  ③**25년 미청구 2만여 건 리스트**(준다고 했으나 아직 안 옴).
- **★지도 불가 135건 — 그대로 둔다**(영준님 2026-09-14). 사유 보존을 위해 남기고 작업자에게 불가앱 작성 통보.
- **★장애 리스트 재산출 — 당분간 안 함**(영준님 2026-09-14).
- **★고객번호가 `CNTR_NO` 인지 주소 괄호 안 번호인지 (미확정)** — 표본이 하나뿐이다.
- **★계약종별로도 안 걸리는 고압 4건** — 작업자가 '고압'으로 불가 찍었는데 계약종별은 주택용 3·가로등(을) 2.
- **★서울본부직할 중구 물량** — 공사번호 필터를 걷어내면서 들어왔고 지금도 계속 온다. 용산은 확인받았으나 중구는 미확인.
- **★글로벌 규약 반영 승인 2건 (감사 대기)** ①포트 레지스트리(8765 → 8000대 이전 권고)
  ②데이터 계약(공유 산출물=단일 소유자·소비자 읽기전용·스키마 변경 사전통보)
- **★ 한전 데이터 열람 포털 자동화 (미조사)** — 권한자(김창숙 사장님) OTP 필요. 30분 sliding 세션.
- **★ 22차 중복 관철동 11-14(02470001147) awms 관리자 삭제요청** — 완료(28)+작업자권한이라 현장삭제 불가.

## 진행중 트랙 (요약 — 상세는 옵시디언 로그·메모리)
- **Tailscale 인프라 전환** — 남은 것: start.sh tailnet 발행 영구화, 검증관리자 8765 tailnet화, A31 적용.
- **계기큐 모니터 재구조화** — P4(임시저장25 1건 실등록 검증)·P5(기존 로직 제거)는 실운영 로그인 때.
- **검증관리자 데이터워크벤치** — 1~4단계 배포완료, 5단계 awms 등록은 별도.
- **종로 보조앱(snap)** — v20260719.4 배포. **남은 GATE: 전파불량 배경 업로드 시나리오 미실증.**
- **OTP수집기(A31)** — 카톡 자동진입 간헐실패 미해결. A31=남의폰이라 USB 불가.

## 블로커
- **★cst-app(아미큐 네이티브) 백업 없음 — 유실 위험.** remote 없는 로컬 전용 repo, 커밋 1개.
  **private repo 만들어 push 필요 — 영준님 승인 대기.**
- **★A33 폰 연결 불가로 2건 정체** — ①헬퍼 fetch 캡처 ②APK 직접설치. **무선디버깅 포트 주시면 즉시 착수.**
- **★아미큐 변대주 DATA_NUM 재전송 실검증 대기** — backend 재구동됨, 현장 재전송 결과 미확인.
- **★지식 재구조화 v1 — 영준님 할 일**: 옵시디언에 Obsidian Git 자동백업 설정 + 폰 옵시디언 repo 연결.
- **★`js/auth.js` 평문 클라이언트 인증** — repo 가 public 이라 이미 노출.

## Firebase 요금제/사용량
- **비용 근본해결 3종 완료** — P1 catch-up 델타화 · B2/B3 조건부GET · P3 주간 아카이브.
  launchd 월요일 05:00 자동화(`com.ami.jongno-weekly-merge`). [[firebase_cost_rtdb_catchup]]
- ami-work Storage 안 씀(사진은 ami-jongno만).

## 핵심 규칙 (사고 방지 — 영구)
- **★독립 repo는 worktree로 쪼개지 않는다** — `jongno-combined`·`cst-app`·`jongno-snap`·`awms-helper`.
  단독 소유 = **cst-app·awms-helper=통신팀 / jongno-combined·jongno-snap=계기팀**.
- **★자격증명 파일은 절대 커밋 금지 — repo 가 public + Pages 배포다.**
- **★아미큐 화면은 전부 네이티브(cst-app)** — `ami-queue-www/`는 죽은 옛 웹뷰. 수집·QR=`CollectScreen.kt`.
  **폰 UI 손대기 전에 백엔드에서 awms 원시응답을 덤프로 실측할 수 있는지 먼저 보라.**
- **★헬퍼는 push 만으로 반영된다**(Pages 의 `awms-bridge-inject.js` 원격로드). **아미큐는 네이티브라 빌드 필요.**
- **awms 맥변경 = 모뎀 재결합(saveAct 아님), 마스터 먼저.** 통신팀/계기팀 교차호출 405.
- **종로팀 awms 아이디 = mdp2504271 전용 고정.**
- **★Pages 배포 = APK·웹무관 제외.** APK 는 `raw.githubusercontent.com/815dudwns/ami-work/main/파일명.apk`.
- **★작업앱(접근성/오버레이) = 금융앱 피싱탐지에 악성 오탐.** 영준님 "냅둬".
- **TOU = 정적파일(data/tou-data.json), Firebase 아님.**
- **★site-data.json 변경 시 `gen_site_version.py` + `gen_stats_index.py` 필수.**
  `upload_new_data.py`는 workStatus 를 통째 교체하므로 **실행 금지**.
- **아미맵 마커 = 좌표 기준 그룹핑**(6자리). 합동은 한 건물 좌표가 완전히 같아 마커 1개로 묶인다.
  **통계 개소는 좌표가 아니라 지번**(`jibunOf`) — 지오코딩 오차로 갈리거나 뭉치는 것을 피한다.
- **종로맵 검침값 입력규칙** — 자릿수 단상5·나머지6, 최대전력만 7. 순서 주·야·무효·최대.
- **28(완료) 되돌리기 불가**(계기팀). 통신팀은 전송 전이면 삭제·수정 자유.
- **★계기큐 재구조화/데이터관리자 전송 = 임시저장(25)까지만, 완료(28) 절대금지.**
- **주소상태(workStatus)는 무조건 Firebase.** 종로DB=**ami-jongno**(ami-work 아님).
- **★실효계기 단일원본 = `data/site-data.json`. stats 분모 = `data/stats-site-index.json`(정적).**
  **종로/철거 대조 키=고객번호**(계기번호는 교체로 바뀐다).
- **ami-work/jongno 코드는 PM 직접 수정 가능**. 단 아미맵 실무는 데스크 발주가 원칙.
- **AUTH/FORCE_LOGOUT_VERSION 평소 배포에 건드리지 말 것.**
- **★인프라**: `.git` 5.6GB. 남은 정리(미착수) — `jongno-combined/apk/` 옛 APK 약 900MB /
  APK 배포를 **GitHub Releases 로 전환**.
