# HANDOFF — ami-work(아미맵) / jongno-combined(종로맵) / 계기큐·아미큐·헬퍼

> ★ **이 문서는 영구 상태판이다. 휘발성 세션 진행메모를 여기 쓰지 말 것** — 작업 디테일·과정은 메모리(`~/.claude/.../memory/`)와 `research/` 문서에 남긴다.
> ami-work 세션이 여러 개라 충돌하므로, HANDOFF는 **잘 안 바뀌는 것**(미완 트랙·대기 액션·핵심 규칙)만 단일 권위로 유지한다. **완료·배포·검증된 건 여기 남기지 말고 즉시 지운다**(상세는 옵시디언 로그·메모리).
>
> 앱 통칭 [[app_naming_convention]]. 배포 시 APP_VERSION 갱신 [[jongno_app_version_deploy]]. 종로맵=Naver Maps.
> **awms API 모를 때 = `research/awms-poc/awms_API_레퍼런스.md`. 통신팀(mob/cst)/계기팀(mob/mtr) 별개 — 항상 구별.**

## 현재 상태 (2026-09-14)
- **마지막 작업**: 합동 리스트업(9/14분 321건 반영, 지도 971). 그 전 구간에 **실효 리스트 전면 교체**
  (3,969→3,096), **재방문·장애·고압 데이터셋 지도에서 제거**, **아미큐/헬퍼 맥 오류 검사 신설**,
  **델타 삭제반영 버그 수정**, **보강현황 `visible` 열 적재**를 끝냈다.
- **진행 중**: "앱 기록 없이 LP 붙은 건" 조사(아래 `다음 0-A`). 데이터는 다 모였고 판단만 남았다.
- **배포 상태**: ami-work `751aec3` / 아미맵 map.html **v20260914.1** / 아미큐 **2.2.18** /
  헬퍼 inject **v85**·앱 1.0.77 / 종로맵 20260813.3.
- **지도 데이터**: 실효 3,096(지번 1,346곳) · **합동 971**(9/10 395·9/11 272·9/14 304) · SKT 41.
  합동 누적 5,035(백업 4,064). **장애·고압은 `onMap:false`** — 통계 분모엔 남아 있다.
- **데스크**: 통신팀(`tongsin`) 가동 중, 이번 구간 작업 대부분을 수행. 상세 `HANDOFF-통신팀.md`.

### ★이번 구간에 새로 만든 규칙 (되돌리지 말 것)
- **★계기번호 영문자 접두를 지우지 마라** — `A0530188699`·`LA530151258`. 정규화는 **영문자가 섞였으면
  그대로 두고(공백·하이픈만 제거·대문자화), 순수 숫자일 때만 `zfill(11)`**. "엑셀 float→int→str→zfill"은
  **앞 0 복원**이지 숫자만 남기라는 뜻이 아니다. 2026-09-10 에 그렇게 오독해 167건을 뭉갰다([[meter_no_prefix_preserve]]).
  아미큐 `normMeter`·통신팀 합동 빌더가 정답 구현이다 — **새로 짜기 전에 그걸 봐라.**
- **★리스트업 산출물은 반드시 원본과 대조하고 배포한다.** 자기 산출물 안에서만 보는 검사
  ("11자리 아님 0건·중복 0건")는 **뭉갠 값도 통과**시킨다. 빌더에 게이트를 넣었다 —
  계기번호 집합 차집합 양방향·접두 문자 건수·좌표 null 이 어긋나면 **저장하지 않고 종료**한다.
- **★Firebase `workStatus` 직접 수정 시 `syncAt` 필수** — 안 밀면 델타 범위 밖이라 폰이 영영 못 받는다.
  `state`·`updatedAt` 만 바꾸면 서버는 맞는데 화면이 안 바뀐다([[firebase_workstatus_syncat_required]]).
- **★한전 엑셀 `visible` 적재 완료** — `xlsx_to_db.py` 가 sheet XML 의 `hidden="1"` 을 읽어
  `visible`(1/0)·`xl_row` 로 싣는다. openpyxl 로는 숨김을 못 본다. 이제 `WHERE visible='1'` 한 줄로
  "그 판의 우리 대상"이 나온다. 보강현황 7개 판 전부 재적재해 원본 행수와 일치 확인.
- **★보강현황 시공기록 열 계보** — 판마다 이름이 바뀐다([[boranggi_sigong_column_lineage]]).
  `col_18`/`col_24`/`플러스_종합`=**플러스앱**(25-03~26-02) · `col_19`·`col_20`=**awms 초기**(26-03~05) ·
  `현장관리`→`26년시공앱`=**awms 26년**(6/8~). 9/8판 `26년시공앱`은 모뎀작업리스트와 **초 단위 100% 일치**,
  7/3판 `현장관리`는 99.8%(나머지 26건은 재작업으로 시각이 갱신된 것).
- **★모뎀작업리스트는 계기별 최신 기록만 남는다** — 재작업하면 옛 시공 시각이 덮인다.
  신교동 17-22 는 6/8·7/27·8/28 세 번 갔는데 표에는 두 번만 있다. **실제 시공 횟수는 그 표보다 많다.**
  전체 이력이 필요하면 옛 판의 `현장관리`·`col_19` 를 함께 모아야 한다.
- **★SMGW-C 는 무선 수집** — 근처 게이트웨이가 주변 계기를 끌어간다. **우리 시공 없이 LP 가 붙는 게 정상**이고
  한전 필터(LP 빈칸)가 그래서 걸러낸다. 같은 건물 일부만 LP 가 붙었으면 나머지만 대상이다([[smgwc_wireless_pickup]]).
- **★합동 수집 구간 = 지난 수집 시점 ~ 이번 수집 명령** — 날짜 하나만 조회하면 밤 등재분이 영영 빠진다.
  마포용산·서울본부직할은 22~24시에도 올린다. `fetch_awms.py` 가 `last_fetch.json` 으로 구간을 잡는다.
  실측: 9/9 저녁 수집 369건 → 다음날 재조회 393건(+24, 전부 마포용산 23시대)([[hapdong_collect_window_rule]]).
- **★통계 인덱스는 main 워크트리에서만 생성한다** — `archivesGlob` 이 워크트리 기준이라 데스크에서 돌리면
  **없는 파일이 조용히 0으로** 잡힌다. 실측 2026-09-09: 완료 아카이브가 26,204→2,076 으로 깎인 인덱스가 배포됐다.
  지금은 `gen_stats_index.py` 에 **축소 시 `exit 1`** 가드가 있다. 7/4 아카이브는 저장소에 넣었다(`9d94715`).
- **★모뎀맥 법칙 (아미큐·헬퍼 공통)** — 글자는 **0-9·A-F 만**(모든 타입). LTE류 `012`+숫자 8자리=11자리,
  DCU류 hex 12자리. 앞 6자리는 **847207·E0AEED·AC5E8C·44B433·0014B0** 다섯뿐이고 847207 이면 7번째는
  0·B·C·D·E. 어기면 **[맥 오류]** 박스. 손입력 중엔 안 뜨고 스캔·[다음] 때만 판정한다.
  정본은 `cst-input/backend/app.py` `MAC_OUI` — 새 모뎀이 오면 거기·CollectScreen.kt·inject 셋을 같이 고친다.
- **★고압은 계약종별로만 걸러진다 — 목록은 코드표에서 읽는다** — `DIST_LV_HV_CLCD` 는 전 건 저압으로 와서 못 쓴다.
  빌더가 `awms_code`(`p_code='CI007'` 중 명칭에 '고압')에서 읽고 손으로 적은 18개는 폴백이다.
  **빈 필터로는 진행하지 않는다.** 코드표 갱신 `python3 scripts/fetch_awms_codes.py`(awms 세션 필요 → 수집 직후 함께).
- **★실효 통신방식은 변대주 DCU 대장값** — 한전 `통신방식.1` 은 3,096건 중 3,009건이 빈값이라 못 쓴다.
  대장 `PLC`→**KS-PLC** 로 적고, 대장에 없으면(순수숫자 10자리=LTE 회선번호) **LTE**. HPGP 는 뭉개지 않는다.
  **DCU 대장에 없는 개소는 DCU 정보를 통째로 비운다** — 한전이 LTE 전환 뒤에도 옛 PLC DCU 상태를 실어 보내
  허수 '검침실패'가 318건 있었다.
- **★DCU 매칭은 변대주명 우선** — DCU ID 와 어긋나면 변대주 쪽을 쓴다(전주에 DCU 가 있는데 개소만 LTE 로 단 경우
  DCU ID 가 옛 소속을 가리킨다). 9/8 판에서 61건이 그랬다.
- **아미맵 실무는 통신팀 데스크에 발주한다** — PM 이 직접 하지 마라(영준님 2026-09-09 지적).
  발주서는 `/tmp/relay/<주제>-<날짜>.md` 에 쓰고 경로만 보낸다. **그날 main 이 바뀐 내용을 발주서에 적어라**
  (데스크가 `git merge origin/main` 부터 하도록)([[pm_delegate_amimap_to_desk]]).
- **데이터셋은 `js/datasets.js` 한 곳에서 정의한다** — 지도 마커·카테고리 패널·통계 리스트·통계 인덱스가
  전부 이 레지스트리를 읽는다. 파이썬 거울 `scripts/datasets.py`, 어긋나면 `test_datasets_parity.py` 가 잡는다.
  **지도에서만 내릴 때는 주석 처리가 아니라 `onMap:false`** — 주석 처리하면 통계 분모에서도 빠진다.
- **통계 개소 = 지번(건물) 단위** — 합동 주소는 awms 가 동호수까지 붙여 줘서 주소로 세면 한 건물이 여러 개소가 된다.
- **합동시공은 그날 작업분만, 전지사·공사번호 필터 없음** — 거르는 것은 고압뿐. 과거 누락분은 소급하지 않는다.
  `scripts/hapdong/fetch_awms.py` DEPTS 가 단일 출처. ★스킬 지사표에 있던 공사번호 필터는 **코드에서 9/2에 걷어냈다**
  (마포용산 67건이 잘린 사고). 표만 보고 되살리지 마라.
- **주소가 비면 상태키를 `CONS_TGT_SEQNO|합동` 으로** — 빈 문자열 키를 만들면 주소 없는 건들이 상태를 공유한다.
- **여러 파일을 순서대로 로드하는 JS 는 합쳐서 실행해 봐야 한다** — `node --check` 는 `const` 중복선언(전역 공유)을
  못 잡는다. 그것 때문에 지도가 통째로 죽은 적이 있다(2026-09-04).

### 다음 (우선순위)
0-A. **★"앱 기록 없이 LP 붙은 건" 판단 — 데이터는 다 모였다, 결론만 남았다**
  9/8 판 LP 수신 77,128건 중 **어느 앱에도 기록 없는 것 8,701건(11%)**. 매달 8~16%로 상시 발생한다.
  세 갈래로 갈린다 — ①**아미고+SMGW-C 7,453건**=무선 자동수집(정상, 우리 일 아님) ②**기설**(기존 모뎀 유지,
  교체 당일 LP) ③**누가 달았는데 기록 없음**(문제).
  · **DCU 계열(PLC·K-DCU·HPGP) 573건**이 실질 후보다. 모뎀 MAC 이 hex12 로 실려 있어 **뭔가 달린 것은 확실**하다.
  · 그중 **98건은 우리가 단 맥인데 계기번호가 다르다** — 한 모뎀에 계기 여럿이 물렸는데 마스터만 등록된 것.
  · **6/8 이후 교체 + 맥이 모뎀작업리스트에 없음 = 103건**(PLC 69·K-DCU 30·HPGP 4 / 동대문중랑 37·마포용산 29·
    노원도봉 27). **교체→LP 8일 초과가 69건**이라 기설로 설명이 안 된다. ★이게 가장 설명이 필요한 덩어리다.
  · 한 번이라도 우리 리스트에 떴던 것은 963건(DCU 계열 205건).
  · 16·15자리 맥은 SMGW-C 회선번호를 ASCII hex 로 인코딩한 것(`3439383531343433`→`49851443`→`01249851443`).
    형식은 우리 것과 같은데 **값이 1.3%만 겹친다** — 우리가 단 게이트웨이가 아니라는 뜻.
  ★남은 선택: 주덕기 과장에게 103건을 물을지, 표본을 현장 확인할지, 그냥 둘지.
0-B. **아미큐 교체(M1020) 실전 검증** — 2.2.18 배포했으나 awms 왕복 미확인. 통신팀은 임시저장(25)이 없어
  완료(28)로 1건 올린 뒤 `deleteRows` 해야 한다(전송 29 전이면 삭제 자유).
  첫 교체건 올릴 때 `getDetail` 로 `EXT_FCTY_ID`·`REMV_MEMO`·슬레이브 `WORK_DIV` 확인.
0-C. **맥 오류 검사 실전 반응 확인** — 아미큐 2.2.18·헬퍼 v85 배포했으나 현장에서 [맥 오류] 박스가
  실제로 뜨는지, 오탐이 없는지 미확인. 실측 51,779행 대조로는 오독 132건 검출·정상 51,647건 오탐 0.
0-D. **보류 사유를 필수로 할지 (미결)** — 지금은 선택이다(데스크 판단: "일단 보류부터 찍는 흐름을 막으면 안 된다").
  불가는 종전대로 필수. 영준님 결정 대기.
0-E. **LTE 개소에 붙은 옛 PLC DCU 정리 여부** — 실효는 이번에 비웠으나 장애 데이터셋엔 28그룹 남아 있다.
0-F. **합동 중복 3건**(같은 계기가 두 작업일에 등장) — LP타운 103호는 한전이 같은 호수를 새 순번으로 올린 것.
  상계동 2건은 계기번호 오입력 의심. 주덕기 확인 목록.
1. **PLC 개소 DCUID 입력 표시** — 주덕기 지시 이행, 미구현
2. **아미큐 미결 2건** — ①앱 시작 시 업데이트 자동확인(현재 설정 탭 진입 시에만) ②EXIF 막 올리기 촬영시각 자동정렬
3. **KT모뎀 현장회수 137건** — 우리 개소와 겹치는지 미확인
4. **세션 없이 완료 눌리는 구멍** — `updateStatus()` 세션 가드 없음
5. **검증관리자 주말 수정 대상** — `research/admin-validation/문제점_정리_20260731.md`

### 참고 — 이번 구간 실측 (근거 필요할 때)
- **보강현황 판별 대상 추이**: 5/13 16,490 · 5/28 17,864 · 6/29 22,517 · 7/3 16,485 · 7/30 10,679 ·
  8/28 6,488 · **9/8 3,096**. 줄어든 건 실적이고, 6/29·7/30 에 크게 깎인 것은 **한전이 25년 교체분을
  리스트에서 뺐기 때문**이다(7/30 판부터 2026년 교체분만 온다). 보이는 행은 어느 판이든 **100% LP 미수신**.
- **지사별 앱 작성 시간대** — 서대문은평 8~16시 고르게 / 노원도봉 13~15시 집중 / 동대문중랑 10~11·14~16시 /
  **서울본부직할 16~17시+22시대** / **마포용산 17~23시+자정**. "작업량과 앱 작성이 안 맞는다"는 대개 이것이다.
- **작업자 속도(조 단위, 개소/일)** — 김영진+이규재 44.9 · 우희근+정수화 36.4 · 김지호+장성훈 35.5 ·
  김상권+윤용운 32.8 · 우영준+장진교 31.5. 종합순위(30일 이상 22명): 1 장진교 · 2 이기순 · 3 우영준 · 4 김상권 · 6 윤용운.
- **작업자2 누락은 8월부터 0%** — 6~7월에 이기순·안지홍·장진교·양선왕 등이 빼먹었다(장진교 마지막 7/31).
  그 구간 실적 집계는 짝이 안 적어준 만큼 깎여 있다.
- **아미고(53) 계기는 KS-PLC 에서 약하다** — 전체 KS-PLC 90.2% vs 아미고 86.2%. SMGW-C 에선 97~98%.
- **1차 DCU(접미 00·01)는 성공률이 무너진다** — 2453 KS-PLC 8.3%, 4553 0%. "1차는 원래 안 된다"(영준님).
- **DCU_ID 누락은 LP 실패 원인이 아니다** — 변대주 있고 DCUID 없는 건 89% vs 있는 건 90% vs 둘 다 없는 건 91%.

### ★데이터관리자 설계 확정사항 (유지)
- **봉인**: 7자리 고정 · 박스 범위는 설정 입력 · **계정 무관 팀 자원**(전역 커서) · 다음번호 = 우리 DB 마지막+1,
  awms 조회값은 범위 안이면 반영·밖이면 무시 · 삼상 N,N+1 · 박스 끝 1개 남으면 버림
- **차수**: 하드코딩 금지, 목록 선택. 기본값만 활성차수 자동추종. 화면엔 공사명+차수
- **전송**: 완료(28) 기본 · 검증 자동 대조 · 완료건 읽기 전용 잠금
- **환경**: 봉인 커서 **0315875** / 활성 26차(397820263354) / 계정 mdp2603726 / 백엔드 8765 launchd 자동재기동

### 데스크 상태
| 데스크 | 브랜치 | 상태판 |
|---|---|---|
| 통신팀 | `tongsin` | `HANDOFF-통신팀.md` — 이번 구간 주력 |
| 계기팀 | `gyegi` | `HANDOFF-계기팀.md` |
| 검증팀 | `geomjeung` | `HANDOFF-검증팀.md` |
| ocr-meter | `ocr-meter` | `HANDOFF-ocr-meter.md` |

- **★데스크 상태판 파일명을 갈랐다.** 데스크는 `HANDOFF-<팀>.md`, PM 은 `HANDOFF.md`. **되돌리지 마라** —
  예전엔 같은 경로라 머지하면 PM 상태판이 데스크판으로 덮였다.
- **PM 우편함 `run:run_48e996df65bd`**(`~/.claude/orca-runs.json`). 고아 Run `run_f566b669c5ef` 미회수.
- 워크트리·워크스페이스 삭제 금지(경로 공유 형제 세션 전멸, 2026-07-18·07-19 실증).
- workspaceStatus 는 자동으로 안 바뀐다 — `orca worktree set --workspace-status` 로 직접.

## 대기 액션 (영준님 결정/확인 필요)
- **★주덕기 과장에게 물을 것 3가지** ①**Y열에 모뎀 맥이 찍힌 36건** — 시공앱 기록 없는데 이미 모뎀이 달려 있다
  ②**중랑구 면목동 23-3 계기 1개**(24530088997) — 우리는 미착수인데 9/8 리스트에서 통째로 빠졌다
  ③**25년 미청구 2만여 건 리스트**(준다고 했으나 아직 안 옴). 오면 6/8 **이전** 구간을 보게 되고,
  플러스앱 기록(109,161건)과 대조해 실제 우리 시공분인지 바로 확인된다.
- **★지도 불가 135건 — 그대로 둔다**(영준님 2026-09-14). 우리만 불가로 찍고 awms 불가앱엔 안 올린 것이라
  한전 기준으로는 미착수로 계속 리스트에 나온다. **사유 보존을 위해 남기고 작업자에게 불가앱 작성을 통보**했다.
- **★장애 리스트 재산출 — 당분간 안 함**(영준님 2026-09-14). 재산출하면 14일 기준 851계기(389곳) /
  30일 기준 593계기다. 옛 장애 데이터셋 2,837계기는 미착수 0건이라 지도에서 내렸다.
- **★고객번호가 `CNTR_NO` 인지 주소 괄호 안 번호인지 (미확정)** — 9/7 서대문 건에서 갈렸다. 표본이 하나뿐이다.
- **★계약종별로도 안 걸리는 고압 4건** — 작업자가 '고압'으로 불가 찍었는데 계약종별은 주택용 3·가로등(을) 2.
  계약은 저압인데 현장 인입이 고압인 유형이 얼마나 되는지 확인 필요.
- **★DCU 철거예정 175건은 회선상태가 전부 '개통'** — 정지 196건과 겹침 0. 철거하기로 했는데 해지가 안 된 것인지 확인 필요.
- **★서울본부직할 중구 물량** — 공사번호 필터를 걷어내면서 들어왔고 지금도 계속 온다(9/11 34건).
  용산은 확인받았으나 중구는 미확인.
- **★글로벌 규약 반영 승인 2건 (감사 대기)** ①포트 레지스트리(8765 → 8000대 이전 권고)
  ②데이터 계약(`~/CLAUDE.md` 에 '공유 산출물=단일 소유자·소비자 읽기전용·스키마 변경 사전통보' 추가)
- **★ 한전 데이터 열람 포털 자동화 (미조사)** — awms 아닌 별개 웹포털. 권한자(김창숙 사장님) OTP 필요.
  30분 sliding 세션 → keep-alive 필수. 스켈레톤 `research/kepco-portal/kepco_API_레퍼런스.md`.
- **★ 22차 중복 관철동 11-14(02470001147) awms 관리자 삭제요청** — 완료(28)+작업자권한이라 현장삭제 불가.

## 진행중 트랙 (요약 — 상세는 옵시디언 로그·메모리)
- **Tailscale 인프라 전환** — 아미큐 백엔드 tailnet 전환·A33 무선 adb·OTP adb 직독 실증 완료.
  남은 것: start.sh tailnet 발행 영구화, 검증관리자 8765 tailnet화, A31 적용. [[tailscale_adoption]]
- **계기큐 모니터 재구조화** — P1~P3+가드 완료. **P4(임시저장25 1건 실등록 검증)·P5(기존 로직 제거)는
  실운영 로그인 때.** tailnet 직통 `https://woodelight-imac.tail01244c.ts.net:8765`. [[metercq_monitor_restructure]]
- **검증관리자 데이터워크벤치** — 1~4단계 배포완료, 5단계 awms 등록은 별도. [[validate-workbench-redesign]]
- **종로 보조앱(snap)** — v20260719.4 배포. **남은 GATE: 전파불량 배경 업로드 시나리오 미실증.**
  정밀리뷰 잔여 3건(temp 부분삭제·흡수 경로 하드코딩·검침값 temp 미흡수).
- **OTP수집기(A31)** — 카톡 자동진입 간헐실패 미해결, awmsID 저장 미반영. A31=남의폰이라 USB 불가.

## 블로커
- **★cst-app(아미큐 네이티브) 백업 없음 — 유실 위험.** remote 없는 로컬 전용 repo 이고 커밋이 1개뿐이다.
  맥이 죽으면 앱 소스가 통째로 사라진다. **private repo 만들어 push 필요 — 영준님 승인 대기.**
- **★A33 폰 연결 불가로 2건 정체** — ①헬퍼 fetch 캡처(awms 저장상태 자동 pull) ②APK 직접설치.
  **무선디버깅 포트 주시면 즉시 착수.**
- **★아미큐 변대주 DATA_NUM 재전송 실검증 대기** — backend 재구동됨, 현장 재전송 결과 미확인.
- **★지식 재구조화 v1 — 영준님 할 일**: 옵시디언에 Obsidian Git 자동백업 설정 + 폰 옵시디언 repo 연결.
- **★`js/auth.js` 평문 클라이언트 인증** — repo 가 public 이라 이미 노출. 서버인증 전환은 별도 과제.

## Firebase 요금제/사용량
- **비용 근본해결 3종 완료** — P1 catch-up 델타화 · B2/B3 조건부GET · P3 주간 아카이브(라이브 6.73→1.82MB).
  launchd 월요일 05:00 자동화(`com.ami.jongno-weekly-merge`). [[firebase_cost_rtdb_catchup]]
- ami-work Storage 안 씀(사진은 ami-jongno만). ocr-meter 는 ami-jongno 만 읽음.

## 핵심 규칙 (사고 방지 — 영구)
- **★독립 repo는 worktree로 쪼개지 않는다** — `jongno-combined`·`cst-app`·`jongno-snap`·`awms-helper` 는
  `.gitignore` 로 제외돼 데스크 worktree 에 안 나온다. 단독 소유 = **cst-app·awms-helper=통신팀 /
  jongno-combined·jongno-snap=계기팀**. 착수 전 `git status` 로 남의 미커밋 변경 확인.
- **★자격증명 파일은 절대 커밋 금지 — repo 가 public + Pages 배포다.** `cst-input/backend/config.json`
  (awms 계정 평문)·session.json 과 백업 패턴 전부 ignore 등록됨. **새 설정/세션 파일은 먼저 .gitignore 에.**
- **★아미큐 화면은 전부 네이티브(cst-app)** — `ami-queue-www/`는 죽은 옛 웹뷰, **고쳐도 폰 반영 0**.
  수집·QR=`CollectScreen.kt`, 설정=`SettingsScreen.kt`. **폰 UI 손대기 전에 백엔드에서 awms 원시응답을
  덤프로 실측할 수 있는지 먼저 보라** — 대부분의 필드 확정은 APK 재빌드 없이 끝난다. [[amiqueue_native_meter_qr]]
- **★헬퍼는 push 만으로 반영된다** — `MainActivity.java:64` 가 Pages 의 `awms-bridge-inject.js` 를 원격로드.
  APK 재빌드 불필요. 반면 **아미큐는 네이티브라 빌드 필요**(빌드 후 `cst-version.json` 범프 → 인앱 자동업데이트).
- **awms 맥변경 = 모뎀 재결합(saveAct 아님), 마스터 먼저.** 통신팀(mob/cst)/계기팀(mob/mtr) 교차호출 405.
- **계기큐 코드수정 = ami-work/awms-queue-www push**(APK빌드 아님). 네이티브(카메라/파일선택)만 빌드.
- **종로팀 awms 아이디 = mdp2504271 전용 고정.** 봉인값·차수는 계정 전역이라 한 계정 내선 그대로 이어짐.
- **종로맵 배포 시 APP_VERSION 갱신**(map.html/stats.html 통일) — 안 하면 옛화면 잔존.
- **★Pages 배포 = APK·웹무관 제외** — `*.apk`·research/·scripts·`*.py`·`*.xlsx` 제외(artifact 122MB→~25MB).
  **APK 는 `raw.githubusercontent.com/815dudwns/ami-work/main/파일명.apk` 로 받는다.**
  배포 실패 시 `rerun --failed` 말고 **빈 커밋으로 fresh run**.
- **★작업앱(접근성/오버레이) = 금융앱 피싱탐지에 악성 오탐** — 토스 피싱제로가 헬퍼·아미큐·계기큐·OTP수집기를
  원격제어 사기앱으로 감지. 영준님 "냅둬"(대응 보류).
- **TOU = 정적파일(data/tou-data.json), Firebase 아님 — push로 반영.**
- **★site-data.json 변경 시 `gen_site_version.py` + `gen_stats_index.py` 필수.** 전자는 폰 IDB 캐시,
  후자는 통계 분모. ★`upload_sitedata.py`는 **없는 스크립트**(옛 기록 오류). `upload_new_data.py`는
  workStatus 를 통째 교체하므로 **실행 금지**.
- **아미맵 마커 = 좌표 기준 그룹핑**(같은 좌표 여러 지번 1마커). 완료 시 구성 지번 전부 기록.
- **종로맵 검침값 입력규칙** — 자릿수 단상(17/19/25/26/27/53)5·나머지6, 최대전력만 7. 순서 주·야·무효·최대.
- **28(완료) 되돌리기 불가**(계기팀). 통신팀은 전송 전이면 삭제·수정 자유.
- **★계기큐 재구조화/데이터관리자 전송 = 임시저장(25)까지만, 완료(28) 절대금지** — `no_complete=True` 하드코딩.
- **주소상태(workStatus)는 무조건 Firebase.** 종로DB=**ami-jongno**(ami-work 아님). 삭제경고 [[jongno_delete_protect]].
- **★실효계기 단일원본 = `data/site-data.json`. stats 분모 = `data/stats-site-index.json`(정적).**
  Firebase siteData 는 아미큐 조회용. **종로/철거 대조 키=고객번호**(계기번호는 교체로 바뀐다).
- **ami-work/jongno 코드는 PM 직접 수정 가능**(에이전트 권한거부). 단 아미맵 실무는 데스크 발주가 원칙.
- **AUTH/FORCE_LOGOUT_VERSION 평소 배포에 건드리지 말 것** — 잦은 범프가 "앱 초기화" 원인.
- **awms 로그인 OTP 자동입력** = 카톡 OTP를 네이티브 접근성 서비스가 읽어 webview 에 입력.
  로그태그 `AWMS_OTP`(수집기 `OTPCOL`). [[awms_otp_amiqueue_embed]]
- **★인프라**: `.git` 5.6GB. 남은 정리(미착수) — `jongno-combined/apk/` 옛 APK 약 900MB 삭제 /
  APK 배포를 **GitHub Releases 로 전환**(안 하면 계속 쌓인다).
