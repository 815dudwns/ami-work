# HANDOFF — ami-work(아미맵) / jongno-combined(종로맵) / 계기큐·아미큐·헬퍼

> ★ **이 문서는 영구 상태판이다. 휘발성 세션 진행메모를 여기 쓰지 말 것** — 작업 디테일·과정은 메모리(`~/.claude/.../memory/`)와 `research/` 문서에 남긴다.
> ami-work 세션이 여러 개라 충돌하므로, HANDOFF는 **잘 안 바뀌는 것**(미완 트랙·대기 액션·핵심 규칙)만 단일 권위로 유지한다. **완료·배포·검증된 건 여기 남기지 말고 즉시 지운다**(상세는 옵시디언 로그·메모리).
>
> 앱 통칭 [[app_naming_convention]]. 배포 시 APP_VERSION 갱신 [[jongno_app_version_deploy]]. 종로맵=Naver Maps.
> **awms API 모를 때 = `research/awms-poc/awms_API_레퍼런스.md`. 통신팀(mob/cst)/계기팀(mob/mtr) 별개 — 항상 구별.**

## 현재 상태 (2026-09-20)
- **마지막 작업**: **25미청구·25년불가 리스트 2종 신설 배포** + **상단 변대주 원천을 `dcu_master` 로 통일**
  (남의 변대주를 끌어오던 345건 제거) + 제외축 3종 추가(26년 신설·노후계기·계기번호 타입코드 오류).
- **진행 중**: **주덕기 과장 회신 대기** — 주소·변대주 1,493건 / 미연계 126건 성격 / 계기번호 3건.
- **배포 상태**: ami-work `0b50f8c` / 아미맵 map.html **v20260920.8** · `FORCE_LOGOUT_VERSION` 20260624a(무변경) /
  아미큐 2.2.18 / 헬퍼 inject v85·앱 1.0.77 / 종로맵 20260813.3.
- **지도 데이터**: 실효 3,166 · **25미청구 8,183**(대기 195) · **25년불가 5,628**(대기 3) ·
  합동 943(아카이브 5,430) · SKT 41 · 고압/장애 onMap:false. **통계 분모 69,977**.
  ★LP무기록은 **철거했다**(파일·레지스트리 삭제). 두 새 리스트는 **전 작업자 공개**(allowGroup 없음).
- **데스크**: 통신팀(`tongsin`) 9/20 마감. 상세 `HANDOFF-통신팀.md`.

### ★이번 구간에 확정한 설계 (되돌리지 말 것)
- **★작업의 실체 = 25년에 달린 모뎀을 26년 공사 모뎀으로 갈아끼워 26년 공사로 청구하는 것.**
  그래서 **LP 는 대상 판정축이 아니다**(영준님 9/18·9/20). LP 값은 작업자가 변대주를 바꾸거나
  LTE 로 갈지 정하는 **현장 재량 판단 재료**로만 디테일에 싣는다.
- **★변대주 필드 = 그 개소가 DCU 로 통신하는 곳인지 판별하는 장치**([[bdju_field_purpose_dcu]]).
  상단은 `dcu_master` 가 **유일한 원천** — DCU ID·통신방식·회선상태·철거판정·차수를 **한 묶음으로**
  가져온다(다섯 동수 확인). 원장 값으로 메우지 마라. **대장에 없다 = DCU 통신이 아니다**(판별 결과).
- **★계기 디테일 = 그 계기에 대한 모든 정보.** 채움률 낮다고 필드를 빼지 마라 — 뺀 것은
  영준님이 지목한 **시공자·DCU장애여부**뿐. 불가 리스트는 **현재 통신방식도 뺀다**(모뎀 미시공이라 뜻이 없다).
- **★상단(대장)과 디테일(원장)은 독립이다.** 상단 PLC · 디테일 LTE 는 모순이 아니다(1,187건 정상).
  서로 비교해 고치지 마라. **판단 유도 문구 금지** — 값만 보여주면 작업자가 안다.
- **★변대주 3축은 끼리끼리만 매칭**([[dcu_match_axis_separation]]) — 이름(한글)/전산화번호(8)/DCU ID(10).
  단 `DCUID 앞8 = 전산화번호` 는 대장 19,007건 예외 0 인 **계산**이라 유도해도 된다.
  **게이트: `python3 scripts/validate_bdju.py <산출물>` — 실패하면 산출물을 내지 마라.**
- **★계기번호 법칙 = 11자리 · 1~2번째만 영문 · 3~4 타입코드 · 5~11 숫자.** 위반은 오타이고
  **대장에 안 붙어 '미처리 유령 건' 으로 남는다** — MAC·고객번호로 역추적하면 이미 끝난 일인 경우가 많다
  (9/20 실측 9건 중 6건). **자동 교정 금지.**
- **★데이터에 있는 것과 화면에 보이는 것은 다르다.** 9/20 에 불가사유 5,694·DCU통신방식 9,121 이
  데이터엔 있는데 `detail.js` 가 안 그려 통째로 빠져 있었다. **적재와 렌더를 따로 확인하라** —
  `node scripts/verify_detail_render_20260920.js` 가 필드별 '데이터 보유 vs 화면 출현' 을 센다.
- **★외부 메일 발송은 "보내" 라는 지시가 있을 때만.** 9/20 에 "합쳐" 를 승인으로 읽고 보냈다.
  Gmail API 발송은 **취소·회수 불가**(SENT 즉시 전달).
- **★엑셀 앞 0 은 자릿수로 판단해 복원**(CLAUDE.md 표) — 계기 11 · 고객번호 10 · 변대주번호 8 ·
  DCU ID 10 · 봉인 7. **MAC 은 11/12 혼재라 zfill 금지.**
- **★DB 적재는 `xlsx_to_db.py` 가 정본**(9/20 수정). 계기 접두 보존 + 고객번호 `norm_cust`.
  통신팀판에만 있던 **보조 고객번호 열 coalesce** 도 합본됐다(고객번호가 두 칸으로 오는 판 대응).

### 다음 (우선순위)
1. **★주덕기 과장 회신 처리** — ①주소·변대주 1,493건 ②**미연계 126건이 모뎀 교체 대상인지**
   ③계기번호 3건(`2519B153734`·`255300B3580`·`45530L93990`, MAC 은 우리가 보유).
   회신 오면 `scripts/promote_michunggu_addr_20260918.py` 로 승격(계기번호+MAC 둘 다 맞을 때만).
   미연계는 `EXCLUDE_MIYEONGYE` 플래그 하나로 되살린다(제외분 `research/미청구_미연계제외_20260920.json`).
2. **★W='제외' 882건 처리 미결** — 26년에 **이미 26년 자재로 갈아끼운** 건(MAC 774 변경·702 개통).
   과장은 "자재 때문에라도 신규 시공해야" 의견. 제외가 맞아 보이나 영준님 판단 대기.
3. **26년 불가 겹침 14건** — 미미해서 그대로 둬도 됨(대상 5 · 제외 1 · 분류표 밖 8).
4. **앱 누락 통보 잔여** — 불가앱 조은규 1건(영준님: 불가앱은 통보 안 함). 시공앱 24건은 9/18 발송 완료.
5. **아미큐 교체(M1020) 실전 검증** — 2.2.18 배포했으나 awms 왕복 미확인.
6. **맥 오류 검사 실전 반응 확인** — 아미큐 2.2.18·헬퍼 v85 현장 오탐 여부 미확인.
7. **보류 사유 필수화 (미결)** · **PLC 개소 DCUID 입력 표시**(주덕기 지시, 미구현).
8. **아미큐 미결 2건** — ①앱 시작 시 업데이트 자동확인 ②EXIF 촬영시각 자동정렬.
9. **세션 없이 완료 눌리는 구멍** — `updateStatus()` 세션 가드 없음.

### ★제외축 정리 — 25년 미청구·불가 (영준님 9/20 "제외한 리스트 정리 잘해")
> 왜 빠졌나를 되짚는 자리. 제외 이력 DB 는 **만들지 않았다**(PM 판단 — 파일과 빌더로 재현되니 과하다).
> 한 건을 되살리려면 해당 축의 플래그/목록만 손대면 된다. 모든 축은 **계기 단위**.

**25년 미청구 — 13,182 → 8,183**(지도 8,183 · 주소대기 195, 합 8,378)

| 순 | 축 | 건수 | 판정 방법 | 제외분 / 되살리기 |
|---|---|---|---|---|
| 1 | 고압 | 637 | 한전 회신 고압 표기. 건물 변압기 모자분리라 DCU 계통 아님([[gapap_high_voltage_nature]]) | 과장과 판단 일치. 되살릴 일 없음 |
| 2 | Sheet2 | 2,612 | 원본 2번 시트 = 청구 근거 없는 건 | 과장과 판단 일치 |
| 3 | 26년 보강 시공 | 1,414 | `modem_work_all` 에 **계기번호 직접** 매칭, 작업일 6/8~9/18 | 이미 26년 자재로 시공됨 |
| 4 | 미연계 | 114 | 과장 9/20 자료에서 분리 제공 | `EXCLUDE_MIYEONGYE` 플래그 · `research/미청구_미연계제외_20260920.json` · **과장 회신 오면 되살릴 수 있다** |
| 5 | 계기번호 오타 | 4 | 형태 법칙 위반 + MAC·고객번호 역추적으로도 특정 실패 | `validate_bdju.py` `METER_PENDING` · 3건은 과장에게 문의 중 |
| 6 | 26년 신설(고객번호 경유) | 23 | 계기번호로는 안 걸리는데 **고객번호로 보강현황을 거쳐** 그 개소의 다른 계기가 `modem_work_all` 에 있는 것. **다리는 보강현황의 (철거) 계기 열만** — 신설 열을 같이 쓰면 과잉 매칭(실측 +9건) | `research/미청구_고객번호경유_26년신설제외_20260920.json` |
| 7 | 계기번호 타입코드 오류 | 2 | 3~4번째 코드가 **대장(boranggi 66만행)에 1건도 없는 것**(21·22·24·51·99). 코드 집합은 대장에서 뽑아 쓴다(하드코딩 X), 표본 5건 미만은 로그로 찍어 사람이 본다 | `research/계기번호오류제외_20260920.json` |

**25년불가 — 11,015 → 5,628**(지도 5,628 · 주소대기 3, 합 5,631)

| 순 | 축 | 건수 | 판정 방법 | 제외분 / 되살리기 |
|---|---|---|---|---|
| 1 | 고압 | 531 | 미청구와 동일 | — |
| 2 | 불가사유 분류 | 4,481 | 사유 959조합을 **대상/제외**로 분류. 영준님 101종 직접 + 5건 미만은 PM | **판정 정본** `research/불가사유_영준님분류_20260920.json` · 전량 `research/불가사유_분류_확정_20260920.json` |
| 3 | 중복 | 157 | awms·실효·합동·미청구와 겹침 | — |
| 4 | 14·15년 노후계기 | 24 | 불가사유 **표기 기준**(영준님 9/20) | — |
| 5 | 26년 신설(고객번호 경유) | 1 | 미청구 6번과 같은 축 · 공용 함수 `via_cust_26_hits` | 위 json 에 함께 |
| 6 | 계기번호 타입코드 오류 | 4 | 미청구 7번과 같은 축 · 공용 함수 `meter_code_bad` | `research/계기번호오류제외_20260920.json` |
| 7 | S(표준형) 계기 | 37 | **계기타입 문자열**로 판정(코드 아님 — 35·15·34·14 에 흩어져 있다). S/표준형/S타입/표준형계기 정확일치, 대소문자·공백 무시. 다른 리스트엔 0건 | `research/S표준형제외_20260920.json` |

**분류 기준**(2번 축) = **다시 가거나 26년 자재로 해결되면 대상** / **물리적 불가·설비 파손·대상 소멸이면 제외**.
★사다리필요·고위치·접근불가는 **제외** 쪽이다(헷갈리기 쉽다).
★**기설(M1030)은 제외하지 않는다** — 25년 모뎀에 26년에 계기를 추가로 물린 것이라 **우리가 갈아야 할 모뎀**이다
(MAC 이 그대로면 기설). 신설만 뺀다([[awms_gisul_billing_rule]]).
- **과장 필터 역산**: `제외`882 = 26년재시공일 보유(완전재현) · `신규시공` 6,054 = LP 미달.
  `앱작성신설` 2,338 포함 3,397 은 **재현 실패**(근거가 파일에 없다, 추정하지 마라).
- **awms 주소 회수 축**: `fmpMtr1000/selectList` 를 **지사 통째**로 받아 로컬 조인(91,216행 25초).
  건별 searchVal 은 8초/건이라 못 쓴다. 철거계기(WHM_NO) 매칭이 우리 개소다. 한전 회신과 대조해
  **정확도 99.4%**. 경량 캐시 `data/inbox_hapdong/awms-fmpmtr-slim-20260918.json.gz`(세션 없이 재판정 가능).
- **DB 통합본**: `dcu_master` 19,007 · `modem_work_all` 66,679 · `bulga_work_all` 1,240.
  조회 헬퍼 `scripts/dcu.py <DCUID|번호|이름>`.

### ★데이터관리자 설계 확정사항 (유지)
- **봉인**: 7자리 고정 · 박스 범위는 설정 입력 · **계정 무관 팀 자원**(전역 커서) · 다음번호 = 우리 DB 마지막+1,
  awms 조회값은 범위 안이면 반영·밖이면 무시 · 삼상 N,N+1 · 박스 끝 1개 남으면 버림
- **차수**: 하드코딩 금지, 목록 선택. 기본값만 활성차수 자동추종. 화면엔 공사명+차수
- **전송**: 완료(28) 기본 · 검증 자동 대조 · 완료건 읽기 전용 잠금
- **환경**: 봉인 커서 **0315875** / 활성 26차(397820263354) / 계정 mdp2603726 / 백엔드 8765 launchd 자동재기동

### 데스크 상태
| 데스크 | 브랜치 | 상태판 |
|---|---|---|
| 통신팀 | `tongsin` | `HANDOFF-통신팀.md` — 9/20 마감. 리스트 2종 신설·DB 재적재·dcu_master·회신 대조 완료 |
| 계기팀 | `gyegi` | `HANDOFF-계기팀.md` |
| 검증팀 | `geomjeung` | `HANDOFF-검증팀.md` |
| ocr-meter | `ocr-meter` | `HANDOFF-ocr-meter.md` |

- **★데스크 상태판 파일명을 갈랐다.** 데스크는 `HANDOFF-<팀>.md`, PM 은 `HANDOFF.md`. **되돌리지 마라.**
- **PM 우편함 `run:run_48e996df65bd`**(`~/.claude/orca-runs.json`). 고아 Run `run_f566b669c5ef` 미회수.
- 워크트리·워크스페이스 삭제 금지(경로 공유 형제 세션 전멸, 2026-07-18·07-19 실증).
- **PM 은 유휴 시 `orca orchestration check --wait` 를 백그라운드로 걸어라** — 안 걸면 데스크 보고가
  영준님이 말을 걸 때까지 방치된다(9/20 실증). **같은 Run 에 wait 를 둘 이상 띄우면 `waiter_exists` 로 거부된다.**

## 대기 액션 (영준님 결정/확인 필요)
- **★W='제외' 882건**(위 다음 2번) · **26년 불가 겹침 14건**.
- **★LP 무기록 진짜 미상 88건 — 주덕기 과장에게 물을지 미정**(리스트는 철거했으나 사안은 남음).
- **★awms 계기번호 오타 17건 수정 / 슬레이브 누락 39건 등록 — 할지 미정.**
- **★지도 불가 135건 — 그대로 둔다**(영준님 2026-09-14).
- **★장애 리스트 재산출 — 당분간 안 함**(영준님 2026-09-14).
- **★고객번호가 `CNTR_NO` 인지 주소 괄호 안 번호인지 (미확정)**.
- **★계약종별로도 안 걸리는 고압 4건** · **서울본부직할 중구 물량 미확인**.
- **★글로벌 규약 반영 승인 2건 (감사 대기)** ①포트 레지스트리(8765 → 8000대 이전 권고)
  ②데이터 계약(공유 산출물=단일 소유자·소비자 읽기전용·스키마 변경 사전통보)
- **★ 한전 데이터 열람 포털 자동화 (미조사)** — 권한자(김창숙 사장님) OTP 필요. 30분 sliding 세션.
- **★ 22차 중복 관철동 11-14(02470001147) awms 관리자 삭제요청** — 완료(28)+작업자권한이라 현장삭제 불가.

## 진행중 트랙 (요약 — 상세는 옵시디언 로그·메모리)
- **Tailscale 인프라 전환** — 남은 것: start.sh tailnet 발행 영구화, 검증관리자 8765 tailnet화, A31 적용.
- **계기큐 모니터 재구조화** — P4(임시저장25 1건 실등록 검증)·P5(기존 로직 제거)는 실운영 로그인 때.
- **검증관리자 데이터워크벤치** — 1~4단계 배포완료, 5단계 awms 등록은 별도.
- **종로 보조앱(snap)** — v20260719.4 배포. **남은 GATE: 전파불량 배경 업로드 시나리오 미실증.**
- **OTP수집기(A31)** — 카톡 자동진입 간헐실패 미해결. A31=남의폰이라 USB 불가.

## 블로커
- **★cst-app(아미큐 네이티브) 백업 없음 — 유실 위험.** remote 없는 로컬 전용 repo, 커밋 1개.
  **private repo 만들어 push 필요 — 영준님 승인 대기.**
- **★아미큐 변대주 DATA_NUM 재전송 실검증 대기** — backend 재구동됨, 현장 재전송 결과 미확인.
- **★지식 재구조화 v1 — 영준님 할 일**: 옵시디언에 Obsidian Git 자동백업 설정 + 폰 옵시디언 repo 연결.
- **★`js/auth.js` 평문 클라이언트 인증** — repo 가 public 이라 이미 노출.

## Firebase 요금제/사용량
- **비용 근본해결 3종 완료** — P1 catch-up 델타화 · B2/B3 조건부GET · P3 주간 아카이브.
  launchd 월요일 05:00 자동화(`com.ami.jongno-weekly-merge`). [[firebase_cost_rtdb_catchup]]
- ami-work Storage 안 씀(사진은 ami-jongno만).

## 핵심 규칙 (사고 방지 — 영구)
- **★독립 repo는 worktree로 쪼개지 않는다** — `jongno-combined`·`cst-app`·`jongno-snap`·`awms-helper`.
  단독 소유 = **cst-app·awms-helper=통신팀 / jongno-combined·jongno-snap=계기팀**.
- **★자격증명 파일은 절대 커밋 금지 — repo 가 public + Pages 배포다.**
- **★아미큐 화면은 전부 네이티브(cst-app)** — `ami-queue-www/`는 죽은 옛 웹뷰. 수집·QR=`CollectScreen.kt`.
- **★헬퍼는 push 만으로 반영된다**(Pages 의 `awms-bridge-inject.js` 원격로드). **아미큐는 네이티브라 빌드 필요.**
- **awms 맥변경 = 모뎀 재결합(saveAct 아님), 마스터 먼저.** 통신팀/계기팀 교차호출 405.
- **종로팀 awms 아이디 = mdp2504271 전용 고정.**
- **★Pages 배포 = APK·웹무관 제외.** APK 는 `raw.githubusercontent.com/815dudwns/ami-work/main/파일명.apk`.
- **★작업앱(접근성/오버레이) = 금융앱 피싱탐지에 악성 오탐.** 영준님 "냅둬".
- **TOU = 정적파일(data/tou-data.json), Firebase 아님.** ★현재 그 파일은 repo 에 없고 `datasets.js` 도 주석 처리 상태.
- **★site-data.json 변경 시 `gen_site_version.py` + `gen_stats_index.py` 필수.**
  `upload_new_data.py`는 workStatus 를 통째 교체하므로 **실행 금지**.
- **★통계 인덱스는 main 워크트리에서만 생성**([[stats_index_main_worktree_only]]). **줄어들면 멈춘다** —
  의도한 감소일 때만 `--allow-shrink`. 합동×실효 중복 167건은 **통계에서만** 실효로 귀속(지도는 그대로).
- **아미맵 마커 = 좌표 기준 그룹핑**(6자리). **통계 개소는 좌표가 아니라 지번**(`jibunOf`).
- **종로맵 검침값 입력규칙** — 자릿수 단상5·나머지6, 최대전력만 7. 순서 주·야·무효·최대.
- **28(완료) 되돌리기 불가**(계기팀). 통신팀은 전송 전이면 삭제·수정 자유.
- **★계기큐 재구조화/데이터관리자 전송 = 임시저장(25)까지만, 완료(28) 절대금지.**
- **주소상태(workStatus)는 무조건 Firebase.** 종로DB=**ami-jongno**(ami-work 아님).
  **★새 리스트는 `NAMESPACED_CATEGORIES` 에 넣어라** — 안 넣으면 같은 주소의 남의 완료 기록을 물려받는다
  (9/20 실증: 미청구가 실효 완료 1,252계기를 완료로 읽고 있었다).
- **★실효계기 단일원본 = `data/site-data.json`. stats 분모 = `data/stats-site-index.json`(정적).**
  **종로/철거 대조 키=고객번호**(계기번호는 교체로 바뀐다).
- **ami-work/jongno 코드는 PM 직접 수정 가능**. 단 아미맵 실무는 데스크 발주가 원칙.
- **AUTH/FORCE_LOGOUT_VERSION 평소 배포에 건드리지 말 것.**
- **★한전 엑셀은 `data/inbox_jdg_<받은날짜>/` 에 원본 보관 + `xlsx_to_db.py` 로 `data/ami.db` 적재.**
  조회는 `scripts/db.py`·`scripts/dcu.py` 또는 생 SQL. **DB 1.6GB, 인덱스 121개.**
- **★인프라**: `.git` 5.6GB. 남은 정리(미착수) — `jongno-combined/apk/` 옛 APK 약 900MB /
  APK 배포를 **GitHub Releases 로 전환**.
