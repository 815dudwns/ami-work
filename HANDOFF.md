# HANDOFF — ami-work(아미맵) / jongno-combined(종로맵) / 계기큐·아미큐·헬퍼

> ★ **이 문서는 영구 상태판이다. 휘발성 세션 진행메모를 여기 쓰지 말 것** — 작업 디테일·과정은 메모리(`~/.claude/.../memory/`)와 `research/` 문서에 남긴다.
> ami-work 세션이 여러 개라 충돌하므로, HANDOFF는 **잘 안 바뀌는 것**(미완 트랙·대기 액션·핵심 규칙)만 단일 권위로 유지한다. **완료·배포·검증된 건 여기 남기지 말고 즉시 지운다**(상세는 옵시디언 로그·메모리).
>
> 앱 통칭 [[app_naming_convention]]. 배포 시 APP_VERSION 갱신 [[jongno_app_version_deploy]]. 종로맵=Naver Maps.
> **awms API 모를 때 = `research/awms-poc/awms_API_레퍼런스.md`. 통신팀(mob/cst)/계기팀(mob/mtr) 별개 — 항상 구별.**

## 현재 상태 (2026-09-18)
- **마지막 작업**: **주덕기 과장 9/17 메일 5첨부 전량 분석 — 25년 미청구 15,069건 구조 규명**
  (청구조건·Sheet2 필터·기설 의미 전부 역산) + **실효 3,166 + 합동 9/17 반영 푸시** + **주 과장에게 6개 요청 메일 발송**.
- **진행 중**: **주덕기 과장 회신 대기**(최신 LP·최신 앱작성 데이터·변대주/주소·불가 재작업 승인·고압 제외).
- **배포 상태**: ami-work `9c0f927` / 아미맵 map.html **v20260917.1** / 아미큐 **2.2.18** /
  헬퍼 inject **v85**·앱 1.0.77 / 종로맵 20260813.3.
- **지도 데이터**: 실효 **3,166**(기존 3,096 + 보강현황 9/17 70) · 합동 1,132 · LP무기록 2,054 · SKT 41
  = onMap 6,393. 합동 아카이브 5,035. **통계 분모 56,127**.
- **데스크**: 통신팀(`tongsin`) 9/17 마감·clear. 상세 `HANDOFF-통신팀.md`.

### ★이번 구간에 새로 밝힌 것 (되돌리지 말 것)
- **★한전 청구 대장은 `신규`·`신설`만 받는다.** 25년 보강 원장에서 청구 판(`작업중` 시트) 진입 조건 =
  `현장구분코드=0 AND 구분∈{신규,신설}`, 258,394건 **반례 0**으로 재현. **기설·철거·재방문은 진입 자체가 안 된다.**
  역산 정본 `research/미청구-필터역산-보고-20260917.md` · 재현 `scripts/미청구-필터역산-재현-20260917.py`.
- **★기설(M1030) = 기존 모뎀에 합류시키는 작업이라 청구 대상이 아니다**([[awms_gisul_billing_rule]]).
  영준님 정의 — 대상 3개인데 가보니 계기 5개, 2개는 이미 돌아가는 모뎀에 물려 있고 나머지 3개를
  **그 모뎀에 합류시키는 것**이 기설. 모뎀을 새로 안 썼고 그 모뎀은 이미 청구됐다.
  **★슬레이브는 모뎀이 아니라 마스터 계기에 485로 붙으므로 항상 `신설`이다** — 기설로 쓴 119건은 앱 실수.
- **★`visible`(숨김/보임)이 곧 한전의 대상 표시다.** 보강현황 9/17판 보임 76 = LP 미수신 /
  미청구2 첫 시트 보임 15,069 = 상태 `미청구` / `작업중` 보임 65,547 = 기성 미수령. **전부 반례 0.**
  `v1` 은 **숨김 0 = 무필터 원본**이고 미청구2는 같은 행에 필터만 건 판정본이다.
- **★주덕기 과장이 보낸 미청구2는 작업 중인 파일이다**(영준님 판단). Sheet2 P열 4,396셀 `#N/A` ·
  `작업중` 대상열 `#N/A` 10,910 · 456MB 시트에 수식 5개(값만 붙여넣기) · 빈 껍데기 1,742행 ·
  재현식 예외 5건(수기 추가). **`#N/A` 원인은 우리가 못 푼다 — 참조 원본이 그의 PC에 있다.**
- **★`#N/A` 는 매칭 실패가 아니라 조회 자체가 안 된 것** — LP 도 `#N/A`, 기성 매칭도 `#N/A` 로 같이 움직인다.
- **★한전이 대상에서 빼도 우리 실효는 내리지 마라.** 9/17판에서 9/8판 3,096건이 전량 숨김 처리됐지만
  그중 **555건이 우리 기준 미완료**(불가 222·미착수 212·보류 121)였다. PM 이 "내려라"로 오발주했다가 원복했다.
  **새 리스트는 교체가 아니라 추가다.**
- **★제외구역(은평·중구·용산)은 합동 리스트에만 적용**([[map_complete_exempt_regions]]). 실효엔 쓰지 마라.
- **★25년 미청구와 26년 실효는 연도 다른 별개 사업이고 26년이 우선**([[boranggi_25_26_priority]]).
- **★보강현황 `현장앱`·`현장앱불가` 열로 앱 누락을 검출한다**([[boranggi_app_omission_detect]]).
  대상에 남은 게 미작업이 아니라 앱 누락일 수 있다. 9/17판 76건 중 32건이 그랬다.
- **★`xlsx_to_db.py` 두 곳 고쳤다** — ①같은 날 같은 시트명 두 판(v1/미청구2)을 가르는 DISCRIMINATOR
  ②`<dimension>` 신뢰 금지(`모뎀설치불가개소` 는 실제 291행인데 dimension 이 `A1:V2` 라 **290행이 조용히 사라졌다**).
- **★Gmail 첨부는 대용량 링크와 일반 첨부가 따로다.** 9/17 메일은 대용량 3 + 일반 2 = **5개**였는데
  대용량만 보고 2개를 놓쳤다. `format='metadata'` 로는 첨부가 안 보인다 — **`format='full'` 로 파트를 훑어라.**
- **★awms 시공사진을 맥에서 직접 볼 수 있다** — `getDetail` 은 `DATA_NUM` 이 비어도
  `FLAG·DEPT1·BUSI_NUM·INSTR_NUM·MAC_MODEM·EXT_FCTY_ID` 로 되고, 사진은
  **`/singleFile.innorix?atchFileId=<ATCH_FILE_ID_3~6>`** 로 받는다(3=시공전 4=모뎀맥 5=시공후1 6=시공후2).
  **단 우영준 시공분만 보인다.**
- **★awms 세션은 아미큐 백엔드(127.0.0.1:8766)에서 가져온다.** `pull_session.py`(헬퍼 경로)는 죽어 있다 —
  `cst-input/backend/session.json` 의 JSESSIONID 를 `~/.awms-tokens/fmpmtr.json` 에 복사하면 된다.
  **폴백을 스크립트에 넣는 것은 미착수.**
- **★계기번호 영문자 접두를 지우지 마라**([[meter_no_prefix_preserve]]). **★리스트업 산출물은 원본과 대조**(차집합 양방향·접두 건수·좌표 null).
- **★Firebase `workStatus` 직접 수정 시 `syncAt` 필수**([[firebase_workstatus_syncat_required]]).
- **★통계 인덱스는 main 워크트리에서만 생성**([[stats_index_main_worktree_only]]). 데스크는 데이터·배포만.
- **★합동 백업은 `data/hapdong-data-archive.json` 에 append**(고정 경로), **보존은 영업일 3일**.
- **★합동 수집 구간 = 지난 수집 ~ 이번 명령**([[hapdong_collect_window_rule]]).
- **★아미맵 실무는 통신팀 데스크에 발주**([[pm_delegate_amimap_to_desk]]). 발주서 `/tmp/relay/`, 경로만 전달.
  ★**단 `/tmp/relay` 는 휘발이다 — 남길 산출물은 repo(`research/`)에 넣어라.**
- **★`data/rework-data.json` 은 폐기됐다** — `apply_dcu_status.py` 는 인자 하나만 받는다. CLAUDE.md 4.5 수정 필요(미착수).
- **데이터셋은 `js/datasets.js` 한 곳**(파이썬 거울 `scripts/datasets.py`). 지도에서만 내릴 때는 `onMap:false`.
- **계정 권한은 `js/auth.js` `AUTH_GROUPS` 한 곳.** 레지스트리엔 그룹 이름만.

### 다음 (우선순위)
1. **★앱 누락 32건 작업자 통보** — 목록 `research/앱누락_통보대상_20260917.json`.
   시공앱 24(안지홍 16·김상권 7·장성훈 1) / 불가앱 8(신완식 7·조은규 1).
   **한 건물 통째로 빠뜨리는 형태**(제기동 134-22 7건·방학동 531 5건·도봉동 613-14 3건).
   앱만 쓰면 회수된다. 윤용운 반장에게 전달할 요약은 세션 로그에.
2. **★주덕기 과장 회신 처리** — 요청 6건(9/18 00:53 발송, 첨부 2):
   ①3구분 기준 또는 최신 LP ②최신 앱작성 데이터(945건 중복 확인) ③기설 슬레이브 119곳 처리법
   ④변대주명·주소(2,200/800) ⑤불가 11,015곳 재작업 승인 ⑥고압 제외 여부.
   회신 오면 **회수 대상 10,528건을 앱작성/현장조치로 갈라 지도에 올린다.**
3. **25년 미청구 회수 계획** — 대상 확정치:
   미청구 13,182 계기 − Sheet2 2,654(제외) = **10,528**.
   주소 11,332 확보(93.5%)·**787 미상** / 변대주 9,929 확보·**2,190 미상**.
   ★Sheet2 제외 근거 = 기설마스터 2,017(이미 청구된 모뎀) + 플러스 철거 482 + 기설슬레이브 117.
4. **아미큐 교체(M1020) 실전 검증** — 2.2.18 배포했으나 awms 왕복 미확인.
5. **맥 오류 검사 실전 반응 확인** — 아미큐 2.2.18·헬퍼 v85 배포했으나 현장 오탐 여부 미확인.
6. **보류 사유를 필수로 할지 (미결)** — 지금은 선택. 불가는 종전대로 필수.
7. **PLC 개소 DCUID 입력 표시** — 주덕기 지시 이행, 미구현
8. **아미큐 미결 2건** — ①앱 시작 시 업데이트 자동확인 ②EXIF 촬영시각 자동정렬
9. **KT모뎀 현장회수 137건** — 우리 개소와 겹치는지 미확인
10. **세션 없이 완료 눌리는 구멍** — `updateStatus()` 세션 가드 없음

### 참고 — 25년 보강 원장 구조 (근거 필요할 때)
- **원장 287,860행** = 청구 261,776 / 미청구 15,069 / 불가 11,015. **미청구는 전량 서울지역본부.**
- **작성툴이 시대를 가른다**: 플러스=2025 전용(신규·재방문) / **현장관리(awms)=2026 전용(신설·기설·교체)** /
  구시공앱=2026 재시공 전용. 플러스의 `신규` = awms 의 `신설`.
- **현장구분코드**: 0=시공 262,503 · 9=철거 14,086(철거구분과 정확히 일치) · 3=불가 11,271.
- **`작업중` 시트 = 청구 대장**, 보임 65,547 = 기성 미수령. `대상` 열은 기성완료와 1:1 아님(1,594건 어긋남 —
  중복 정산분을 걸러낸 것). **`대상` 54,390 은 전부 `청구` 상태라 우리가 할 일이 없다.**
- **25년 불가 11,015건 사유는 원장 `비고1`·`비고2` 에 전 건 있다**(한전에 물을 필요 없음):
  출입불가 3,032 · 계기불량 878 · 함체불량 780 / 상세 문잠김 2,899 · 사다리필요 408 · 계기못찾음 310 · 기설치 796.
- **`모뎀설치불가개소`(26년 4~6월 290건)는 원장에 없다** — awms 불가는 25년 원장에 안 들어간다(원장 불가는 전량 플러스).
- **미청구 945건은 이미 26년 공사로 재작업됐다**(modem_work 6/8~9/8 대조, LP 정상 577).
  대부분 실효 리스트를 타고 우연히 처리됐고, 90건은 같은 모뎀·같은 날 딸려 작업됐다.

### ★데이터관리자 설계 확정사항 (유지)
- **봉인**: 7자리 고정 · 박스 범위는 설정 입력 · **계정 무관 팀 자원**(전역 커서) · 다음번호 = 우리 DB 마지막+1,
  awms 조회값은 범위 안이면 반영·밖이면 무시 · 삼상 N,N+1 · 박스 끝 1개 남으면 버림
- **차수**: 하드코딩 금지, 목록 선택. 기본값만 활성차수 자동추종. 화면엔 공사명+차수
- **전송**: 완료(28) 기본 · 검증 자동 대조 · 완료건 읽기 전용 잠금
- **환경**: 봉인 커서 **0315875** / 활성 26차(397820263354) / 계정 mdp2603726 / 백엔드 8765 launchd 자동재기동

### 데스크 상태
| 데스크 | 브랜치 | 상태판 |
|---|---|---|
| 통신팀 | `tongsin` | `HANDOFF-통신팀.md` — 9/17 마감·clear. 합동 9/17 반영·실효 원복·미청구 역산 완료 |
| 계기팀 | `gyegi` | `HANDOFF-계기팀.md` |
| 검증팀 | `geomjeung` | `HANDOFF-검증팀.md` |
| ocr-meter | `ocr-meter` | `HANDOFF-ocr-meter.md` |

- **★데스크 상태판 파일명을 갈랐다.** 데스크는 `HANDOFF-<팀>.md`, PM 은 `HANDOFF.md`. **되돌리지 마라.**
- **PM 우편함 `run:run_48e996df65bd`**(`~/.claude/orca-runs.json`). 고아 Run `run_f566b669c5ef` 미회수.
- 워크트리·워크스페이스 삭제 금지(경로 공유 형제 세션 전멸, 2026-07-18·07-19 실증).
- **데스크가 작업 중이면 `terminal send` 가 안 먹는다**(turn_start 미관찰). 큐로는 하향이 안 되니
  **발주서 파일에 적어두고 데스크가 다시 읽게 하라.**

## 대기 액션 (영준님 결정/확인 필요)
- **★주덕기 과장 회신 6건**(위 다음 2번).
- **★LP 무기록 진짜 미상 88건 — 주덕기 과장에게 물을지 미정.** 42건은 한전이 우리 대상으로 줬던 것인데
  우리 기록 없이 모뎀이 달렸다. 나머지 46건은 애초에 우리 대상으로 온 적도 없다.
- **★awms 계기번호 오타 17건 수정 / 슬레이브 누락 39건 등록 — 할지 미정.**
- **★지도 불가 135건 — 그대로 둔다**(영준님 2026-09-14).
- **★장애 리스트 재산출 — 당분간 안 함**(영준님 2026-09-14).
- **★고객번호가 `CNTR_NO` 인지 주소 괄호 안 번호인지 (미확정)**.
- **★계약종별로도 안 걸리는 고압 4건** — 작업자가 '고압'으로 불가 찍었는데 계약종별은 주택용 3·가로등(을) 2.
- **★서울본부직할 중구 물량** — 용산은 확인받았으나 중구는 미확인.
- **★글로벌 규약 반영 승인 2건 (감사 대기)** ①포트 레지스트리(8765 → 8000대 이전 권고)
  ②데이터 계약(공유 산출물=단일 소유자·소비자 읽기전용·스키마 변경 사전통보)
- **★ 한전 데이터 열람 포털 자동화 (미조사)** — 권한자(김창숙 사장님) OTP 필요. 30분 sliding 세션.
- **★ 22차 중복 관철동 11-14(02470001147) awms 관리자 삭제요청** — 완료(28)+작업자권한이라 현장삭제 불가.
- **★Y열 모뎀맥 36건 / 면목동 23-3 계기 1개(24530088997)** — 배경 기록이 유실돼 무슨 질문인지 알 수 없다.
  9/18 메일에서 뺐다. 기억나면 다시 문의.

## 진행중 트랙 (요약 — 상세는 옵시디언 로그·메모리)
- **Tailscale 인프라 전환** — 남은 것: start.sh tailnet 발행 영구화, 검증관리자 8765 tailnet화, A31 적용.
- **계기큐 모니터 재구조화** — P4(임시저장25 1건 실등록 검증)·P5(기존 로직 제거)는 실운영 로그인 때.
- **검증관리자 데이터워크벤치** — 1~4단계 배포완료, 5단계 awms 등록은 별도.
- **종로 보조앱(snap)** — v20260719.4 배포. **남은 GATE: 전파불량 배경 업로드 시나리오 미실증.**
- **OTP수집기(A31)** — 카톡 자동진입 간헐실패 미해결. A31=남의폰이라 USB 불가.

## 블로커
- **★cst-app(아미큐 네이티브) 백업 없음 — 유실 위험.** remote 없는 로컬 전용 repo, 커밋 1개.
  **private repo 만들어 push 필요 — 영준님 승인 대기.**
- **★아미큐 변대주 DATA_NUM 재전송 실검증 대기** — backend 재구동됨, 현장 재전송 결과 미확인.
- **★지식 재구조화 v1 — 영준님 할 일**: 옵시디언에 Obsidian Git 자동백업 설정 + 폰 옵시디언 repo 연결.
- **★`js/auth.js` 평문 클라이언트 인증** — repo 가 public 이라 이미 노출.

## Firebase 요금제/사용량
- **비용 근본해결 3종 완료** — P1 catch-up 델타화 · B2/B3 조건부GET · P3 주간 아카이브.
  launchd 월요일 05:00 자동화(`com.ami.jongno-weekly-merge`). [[firebase_cost_rtdb_catchup]]
- ami-work Storage 안 씀(사진은 ami-jongno만).

## 핵심 규칙 (사고 방지 — 영구)
- **★독립 repo는 worktree로 쪼개지 않는다** — `jongno-combined`·`cst-app`·`jongno-snap`·`awms-helper`.
  단독 소유 = **cst-app·awms-helper=통신팀 / jongno-combined·jongno-snap=계기팀**.
- **★자격증명 파일은 절대 커밋 금지 — repo 가 public + Pages 배포다.**
- **★아미큐 화면은 전부 네이티브(cst-app)** — `ami-queue-www/`는 죽은 옛 웹뷰. 수집·QR=`CollectScreen.kt`.
- **★헬퍼는 push 만으로 반영된다**(Pages 의 `awms-bridge-inject.js` 원격로드). **아미큐는 네이티브라 빌드 필요.**
- **awms 맥변경 = 모뎀 재결합(saveAct 아님), 마스터 먼저.** 통신팀/계기팀 교차호출 405.
- **종로팀 awms 아이디 = mdp2504271 전용 고정.**
- **★Pages 배포 = APK·웹무관 제외.** APK 는 `raw.githubusercontent.com/815dudwns/ami-work/main/파일명.apk`.
- **★작업앱(접근성/오버레이) = 금융앱 피싱탐지에 악성 오탐.** 영준님 "냅둬".
- **TOU = 정적파일(data/tou-data.json), Firebase 아님.** ★현재 그 파일은 repo 에 없고 `datasets.js` 도 주석 처리 상태.
- **★site-data.json 변경 시 `gen_site_version.py` + `gen_stats_index.py` 필수.**
  `upload_new_data.py`는 workStatus 를 통째 교체하므로 **실행 금지**.
- **아미맵 마커 = 좌표 기준 그룹핑**(6자리). **통계 개소는 좌표가 아니라 지번**(`jibunOf`).
- **종로맵 검침값 입력규칙** — 자릿수 단상5·나머지6, 최대전력만 7. 순서 주·야·무효·최대.
- **28(완료) 되돌리기 불가**(계기팀). 통신팀은 전송 전이면 삭제·수정 자유.
- **★계기큐 재구조화/데이터관리자 전송 = 임시저장(25)까지만, 완료(28) 절대금지.**
- **주소상태(workStatus)는 무조건 Firebase.** 종로DB=**ami-jongno**(ami-work 아님).
- **★실효계기 단일원본 = `data/site-data.json`. stats 분모 = `data/stats-site-index.json`(정적).**
  **종로/철거 대조 키=고객번호**(계기번호는 교체로 바뀐다).
- **ami-work/jongno 코드는 PM 직접 수정 가능**. 단 아미맵 실무는 데스크 발주가 원칙.
- **AUTH/FORCE_LOGOUT_VERSION 평소 배포에 건드리지 말 것.**
- **★한전 엑셀은 `data/inbox_jdg_<받은날짜>/` 에 원본 보관 + `xlsx_to_db.py` 로 `data/ami.db` 적재.**
  조회는 `scripts/db.py` 또는 생 SQL. **DB 1.6GB, 인덱스 121개.**
- **★인프라**: `.git` 5.6GB. 남은 정리(미착수) — `jongno-combined/apk/` 옛 APK 약 900MB /
  APK 배포를 **GitHub Releases 로 전환**.
